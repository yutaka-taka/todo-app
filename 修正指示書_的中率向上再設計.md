# 修正指示書 — 的中率向上 全面再設計（v400 シリーズ）

> **対象実装者**: Claude Sonnet 4.6
> **対象環境**: `C:\keiba`（Next.js 14 / Prisma 5 / PostgreSQL 18 / TypeScript / Windows）
> **基準コミット**: `46facbe`（branch: keiba）
> **位置付け**: `改修設計.md`（v343）の **後継**。v343 は §A〜§G の局所改修。本書は **データ取込み範囲 / 学習データ規模 / モデルアーキテクチャ / 推奨ロジック / UI** を 同時に見直す **再設計指示書**。
> **作成日**: 2026-05-13
> **作成者依頼**: ユーザーから「まったく役に立っていない予想ツールです。的中率を向上するための再設計をしてください。なお、JRA開催の全レースを分析用として実績を取り込むように変更してください」

---

## 0. エグゼクティブサマリ

### 0-1. 直近の失敗ケース（ユーザー報告）

```
予想（連対率上位5頭）
1位 12 ヘデントール (55.0%)
2位 7  クロワデュノール (45.0%)
3位 8  シンエンペラー (36.0%)
4位 3  アドマイヤテラ (28.0%)
5位 4  アクアヴァーナル (23.0%)

実結果
1着 7  クロワデュノール
2着 15 ヴェルテンベルク

→ 15番が予想5頭にない（馬連はおろか枠連も外し）
→ 「役に立たない」評価
```

### 0-2. 診断（なぜ取りこぼしたか）

3 つの構造的欠陥が重なって **伏兵連対馬を取りこぼす** 状態になっている。

| #  | 欠陥                                                              | 影響                                         |
| -- | ----------------------------------------------------------------- | -------------------------------------------- |
| ① | **データの偏り**: HorseStat に **G1出走経験馬とその通常戦のみ** を蓄積。新興・ダービー初挑戦の3歳馬・地方/外国遠征馬の通常戦がゼロ | ヴェルテンベルクのような若手伏兵の評価が「データなし馬の25点ベース」しか出せない |
| ② | **モデルの限界**: 重み線形和のヒューリスティック。15+ 因子の高次交互作用（騎手×コース×季節×馬体重×血統 など）が表現できない | 上位3-4頭は当てやすいが「5頭目の伏兵」を当てる解像度がない |
| ③ | **推奨ロジックの単軸化**: 連対率の単純降順 Top5。期待値（オッズ × 連対率）や「平均人気からの乖離」を考慮していない | 上位人気3頭 + 中堅2頭で枠が埋まり、6人気以下の連対馬が常に5位の壁に阻まれる |

加えて、UI バグが 2 件報告されている:

- 馬体重 +/- の入力値が 5頭表示で省略される（推奨表示画面）
- 当日が日曜日でも次の日曜のレースが表示される/G3が表示されない（`races/route.ts:33` で `G1, G2` のみ）

### 0-3. 再設計の3本柱

| 柱  | 領域                | 概要                                                                                             | 期待効果              |
| --- | ------------------- | ------------------------------------------------------------------------------------------------ | --------------------- |
| 第I柱 | **データ層 全面再構築** | JRA 全レース（重賞外含む）を **カレンダー駆動** で網羅取得。馬プロファイル経由の逆引きを廃止 | 学習サンプル 16K → 200K+ |
| 第II柱 | **モデル層 ML 導入** | LightGBM をメイン、既存ヒューリスティックを 30% アンサンブル。ONNX で Node 推論              | blind 78% → **88%+** |
| 第III柱 | **推奨ロジック多軸化** | 「連対率」「期待値」「乖離度」3軸で 5頭を構成。表示も 5頭固定だが内訳を変える               | 馬連的中率 +10pt+   |

数値目標:

| KPI                              | 現状     | 目標     |
| -------------------------------- | -------- | -------- |
| 連対率（5頭中に連対馬1頭以上）   | 78%      | **92%**  |
| 馬連的中（推奨2頭が両方連対）    | 推定 22% | **35%**  |
| 3連複的中（推奨3頭が連対馬2頭）  | 推定 18% | **30%**  |
| データ網羅率（過去5年JRA全レース） | 約 8%    | **100%** |

### 0-4. 既存 v343 改修との関係

`改修設計.md` の §A〜§G（v343）は既に実装され `46facbe` でコミット済み。本書はそれを **置き換えない**:

- v343 で追加した HorseStat フィールド（intervalData/recentPops/courseDistData/avgHorseWeight）は **そのまま LightGBM の特徴量として再利用**
- v343 で実装した `getFinishGapBonus`, `getCourseDistBonus`, `computeOddsGapBonus` 等のヒューリスティックは **アンサンブルの一翼として残す**
- 本書の改修は v343 の **上に重ねる** 増分。v400.0 → v400.5（バグ修正）→ v400.10（データ）→ v400.20（モデル）の段階リリース

### 0-5. 実装順と推定工数

| フェーズ        | 章        | 工数（Sonnet） | 効果（ブラインド精度） |
| --------------- | --------- | --------------- | ---------------------- |
| Phase 0: バグ修正 | §1        | 0.5日          | UX 即改善 / 機能正常化 |
| Phase 1: データ拡充 | §2        | 1.5日（実行1日） | 78% → 82%（特徴量増加効果） |
| Phase 2: 推奨ロジック | §3        | 0.5日          | 馬連 +5pt              |
| Phase 3: ML導入 | §4        | 2.5日          | 82% → 88%+             |
| Phase 4: 自動化 | §5        | 1日            | 運用安定化             |
| Phase 5: 検証   | §6        | 0.5日          | KPI モニタリング        |
| **合計**        |          | **約 6.5日**    | **78% → 88%+**         |

---

# §1. Phase 0 — 即効バグ修正（最優先・ユーザー直接体感）

## 1-1. 日曜日のG1/G2/G3全レース表示

### 1-1-1. 問題

`src/app/api/races/route.ts:33` で `grade: { in: ['G1', 'G2'] }` となっており **G3 が表示されない**。
さらに、ユーザーから「日曜日に日曜のレースがトップ画面に表示されない」報告あり。コードは `dow === 0 → 当日のみ`のロジックは正しいが、**G3 の重賞** が当日・翌日に該当する場合に消える。

### 1-1-2. 修正

ファイル: `src/app/api/races/route.ts`

```ts
// before (33)
grade: { in: ['G1', 'G2'] },

// after
grade: { in: ['G1', 'G2', 'G3'] },
```

### 1-1-3. 並び順の保証

同じファイル `orderBy: [{ date: 'asc' }, { grade: 'asc' }, { name: 'asc' }]` のグレード昇順だと **G1 < G2 < G3** がアルファベット順で偶然正しいが、`{ grade: 'asc' }` は文字列ソートなのでテストで確認。
G3 を末尾にしたいだけならこのまま OK。

### 1-1-4. 当日が日曜の場合の念入り確認

```ts
// races/route.ts の dow === 0 ブロック内
dayStart = startOfDay(today)
dayEnd = endOfDay(today)
```

これでよいが、念のため **タイムゾーン** をチェック。`new Date()` はローカル TZ なので Asia/Tokyo の 0時境界で動くか実機で確認。Vercel に再展開する想定はないため `JST=システムTZ` で十分だが、コメントを足す:

```ts
// JST想定。Windows/Linux いずれもシステムTZが Asia/Tokyo であること
const today = new Date()
```

### 1-1-5. 検証

```bash
# 月曜・水曜・土曜・日曜の4パターンで起動して確認
date && curl http://localhost:3000/api/races | jq '.races | length, .[].grade'
```

期待: 日曜は当日の G1/G2/G3 がすべて表示される。

## 1-2. 馬体重 +/- 表示の網羅

### 1-2-1. 問題

ユーザー報告:「馬体重で＋の増減があった場合にすべて記載されていない」
`src/app/page.tsx` の予測結果カードで、`weightChange` が `+5` のような正値の場合、`String(+5)` が `5` になり `+` が落ちて見える可能性。
また予測 5頭の表示行内でユーザー入力した weight/weightChange を **再表示していない**（入力UIにしか出ていない）可能性が高い。

### 1-2-2. 修正方針

予測結果カードに **馬体重 (前走比)** を必ず表示し、データソースの優先順は:

1. ユーザー入力 (`horseWeightInputs[馬名]`)
2. オッズ取得時に DB 保存済み (`entry.horseWeight`, `entry.weightChange` ※後者は未保存なので新規）
3. なし → 「-」

### 1-2-3. 表示用ヘルパー

`src/app/page.tsx` の予測結果ブロック（5頭の `<div>` ループ）の **馬名行の直下** に追加:

```tsx
function formatWeight(w: number | null | undefined, c: number | null | undefined): string {
  if (w == null) return '-'
  if (c == null) return `${w}kg`
  const sign = c > 0 ? '+' : c < 0 ? '' : '±'
  return `${w}kg (${sign}${c})`
}

// JSX 内
<div className="text-xs text-slate-300">
  馬体重: {formatWeight(
    horseWeightInputs[p.horseName]?.weight ? Number(horseWeightInputs[p.horseName].weight) : null,
    horseWeightInputs[p.horseName]?.weightChange ? Number(horseWeightInputs[p.horseName].weightChange.replace('+','')) : null,
  )}
</div>
```

### 1-2-4. 入力 UI の改善

5頭推奨「直前」に表示している馬体重入力欄について、`+` を入れない数字も「増えた」と解釈されないよう placeholder を `例: +4 / -2 / 0` に変更。

```tsx
// 既存の input:weightChange
placeholder="例: +4 / -2 / 0"
```

### 1-2-5. 検証

- 予測実行 → 5頭の各カードに「馬体重: 480kg (+4)」のように表示されること
- 未入力馬は「馬体重: -」と表示
- ±0 入力時は「馬体重: 480kg (±0)」

## 1-3. §B オッズ乖離の有効化確認

### 1-3-1. 状態確認

`scorer.ts:690` `computeOddsGapBonus` と `:758` の `bonus` 計算は実装済みだが、ヴェルテンベルクの取りこぼしが起きた事実から **重み = 1.0 では効果が薄い** 可能性が高い。
原因仮説:

- 補正幅 (gap≥5 で raw=4) が他の累積ボーナス（合計 ±60 程度）に埋もれる
- `predRank > 5` のとき適用しないので、**6位以下の伏兵を救う方向の補正がない**

### 1-3-2. 修正

`scorer.ts:690` `computeOddsGapBonus` を以下に置換:

```ts
function computeOddsGapBonus(gap: number, predRank: number, oddsRank: number, weightMult: number): number {
  let raw = 0
  // (A) 上位5位までは旧ロジック維持（市場過剰評価のペナルティ）
  if (predRank <= 5) {
    if (gap >= 5)       raw = 4
    else if (gap >= 3)  raw = 2
    else if (gap <= -5) raw = -3
    else if (gap <= -3) raw = -1
  }
  // (B) 6-10位の伏兵にも市場の盲点ボーナスを与える（ヴェルテンベルク救済）
  // 予想6位以下だがオッズ3-7番人気 = 「市場は評価しているがモデルが拾えていない」
  else if (predRank <= 10 && oddsRank >= 3 && oddsRank <= 7) {
    if (predRank - oddsRank >= 4) raw = 6     // 強い乖離
    else if (predRank - oddsRank >= 2) raw = 3
  }
  return Math.round(raw * weightMult)
}
```

### 1-3-3. 重みデフォルトの引き上げ

`DEFAULT_WEIGHTS.oddsGapMult` を `1.0 → 1.5` に変更。

```ts
// scorer.ts:65
oddsGapMult:          1.5,  // 旧 1.0 → 1.5: 伏兵検出を強化
```

### 1-3-4. 検証

`scripts/full_blind_optimize_v2.js` で oddsGapMult を 0.5/1.0/1.5/2.0 でグリッド最適化し、blind 連対率を測定。最高値を採用。

```bash
node scripts/full_blind_optimize_v2.js --grid=oddsGapMult:0.5,1.0,1.5,2.0 --save
```

`full_blind_optimize_v2.js` がこのフラグに非対応なら、**コード末尾の GRID_OPTIONS 配列に `oddsGapMult: [0.5,1.0,1.5,2.0]` を追加** すること。

---

# §2. Phase 1 — JRA全レース完全取り込み（データ層再構築）

## 2-1. 設計思想

### 2-1-1. 現状（プロファイル駆動）の限界

既存の `scripts/backfill_raceresult_from_horses.js` は:

```
[既知の馬名] → /horse/result/{horseId}/ → その馬の全戦績行
```

の流れで取り込む。HorseStat 既登録馬しか対象にならず、**今週新登場の馬・初挑戦の3歳馬は永遠にゼロデータ** のまま。

### 2-1-2. 新設計（カレンダー駆動）

```
[毎週末の開催日] → /race/list/{date}.html → 全レースID一覧
        ↓
[各レースID] → /race/result/{raceId}/ → 出走馬全頭の結果行
```

これにより:

- 毎開催の **全レース全頭** を取りこぼしなく取り込める
- 通常戦・OP・条件戦・新馬戦すべてカバー
- 翌週走る伏兵馬も「先週の通常戦」のデータを持つ

### 2-1-3. データ規模見積

| 期間              | 開催週末 | 1週末平均レース数 | 1レース平均頭数 | 推定 RaceResult 行数 |
| ----------------- | -------- | ----------------- | --------------- | -------------------- |
| 2014-01〜2026-05  | 約 600   | 36-72             | 12-18           | 約 350,000-600,000   |

現状 RaceResult が 16,321 行なので **+20倍〜+35倍** のデータ拡充。

### 2-1-4. 取込み戦略

| フェーズ | 期間              | 目的                     | 工数        |
| -------- | ----------------- | ------------------------ | ----------- |
| 2-A      | 過去 5 年（2021-01-01 以降） | LightGBM 訓練の中核データ | 1日（実行） |
| 2-B      | 直近 1 年         | 推奨/blind の足元精度    | 4時間       |
| 2-C      | 2014-2020         | 古いデータの長期傾向     | 2日（バックグラウンド） |
| 2-D      | 増分（毎週月曜）  | 直近開催の取込み         | 自動化      |

優先度: 2-B → 2-A → 2-C → 2-D。
**重要**: 2-B/2-A 完了で blind 検証を回し、2-C はバックグラウンド継続でよい。

## 2-2. DB スキーマ拡張

### 2-2-1. Race テーブル

```prisma
model Race {
  // 既存
  id, name, date, venue, grade, surface, distance, ...
  
  // 新規追加
  raceNumber     Int?      // R番号（1-12）
  netkeibaRaceId String?   @unique // 例: "202602010801"
  weather        String?   // 晴/曇/雨/雪
  startTime      String?   // "15:40"
  className      String?   // "新馬", "未勝利", "1勝クラス", "2勝クラス", "3勝クラス", "OP", "L", "G3", "G2", "G1"
  prizePool      Int?      // 1着賞金（万円）
  
  @@index([date, venue])
  @@index([className])
}
```

### 2-2-2. RaceResult テーブル

`finishPosition` を nullable にし、出走取消・除外馬も保存できるようにする:

```prisma
model RaceResult {
  // 既存
  id, raceId, finishPosition, horseNumber, horseName, ...
  
  // 既存変更
  finishPosition Int?  // null = 取消・除外・中止
  
  // 新規追加
  scratchReason  String?  // "出走取消" | "競走除外" | "競走中止" | null
  cornerPositions String? // "1-1-1-1" コーナー通過順位
  passingTime    String?  // "12.0-23.5-..."  途中ラップ
  rapidIncrease  Float?   // 上がり3F (秒)
  trainerNote    String?  // 厩舎コメント（任意）
  netkeibaHorseId String? // 馬個体ID（プロファイル参照用）
  
  @@index([netkeibaHorseId])
}
```

### 2-2-3. 新規 model: RaceCalendar（取り込み進捗）

```prisma
model RaceCalendar {
  date        DateTime @id          // 開催日（00:00:00）
  totalRaces  Int                   // この日の総レース数
  fetchedRaces Int     @default(0)  // うち取込み完了
  status      String   @default("pending")  // pending/partial/done/error
  lastFetched DateTime?
  errorLog    String?  @db.Text
}
```

### 2-2-4. マイグレーション

```bash
cd C:\keiba
npx prisma migrate dev --name v400_full_jra_schema
```

### 2-2-5. 既存データへの影響

- `Race.netkeibaRaceId` は既存行で null。後段の取り込みで埋まる
- `RaceResult.finishPosition` は既存全行が NOT NULL データなので互換性問題なし
- 既存 `seed` データの `horseNumber=finishPosition` 問題（[[project_raceresult_backfill]]に記載）は別途 `scripts/cleanup_seed_horsenumber.js` で対応済みのはず。未確認なら `node scripts/cleanup_seed_horsenumber.js --dry-run` で再確認

## 2-3. 新スクリプト: `scripts/fetch_jra_full_calendar.js`

### 2-3-1. 仕様

```
node scripts/fetch_jra_full_calendar.js --from=2021-01-01 --to=2026-05-13 [--dry-run] [--limit=N]
```

| フラグ          | 意味                                          |
| --------------- | --------------------------------------------- |
| `--from=DATE`   | 開始日（含む）                                |
| `--to=DATE`     | 終了日（含む）                                |
| `--dry-run`     | DB 書き込みせず件数のみ                        |
| `--limit=N`     | 取込み対象開催日を最初の N 日に制限            |
| `--resume`      | 進捗ファイルから続きを実行                     |
| `--rebuild-stats` | 完了後に HorseStat を再構築                  |

### 2-3-2. 動作フロー

```
1. 期間内の毎週土曜・日曜の日付リストを生成
2. 各日付について:
   2-1. RaceCalendar を upsert（status=pending）
   2-2. /race/list/{date} を fetch → race_id 一覧抽出
   2-3. 各 race_id について:
        2-3-1. Race を upsert（netkeibaRaceId, name, venue, distance, grade, surface 等）
        2-3-2. /race/result/{race_id} を fetch
        2-3-3. RaceEntry を upsert（出走馬全頭）
        2-3-4. RaceResult を upsert（着順確定の馬すべて）
        2-3-5. 1.5秒 sleep
   2-4. RaceCalendar を status=done に更新
3. --rebuild-stats なら最後に POST /api/reanalyze 相当を直接呼出し
```

### 2-3-3. 実装の詳細

netkeiba パーサは既存 `scripts/backfill_raceresult_from_horses.js` のロジックを **再利用**。
ただしレース結果ページのテーブル構造は馬プロファイルとは異なるため、新パーサ関数を実装:

```js
// scripts/fetch_jra_full_calendar.js（抜粋）

async function fetchRaceList(date /* YYYYMMDD */) {
  const url = `https://race.netkeiba.com/top/race_list.html?kaisai_date=${date}`
  const html = await fetchEucJp(url)
  if (!html) return []
  // race_id=YYYYVVDDRR を抽出（VV=会場コード、DD=開催日番号、RR=レース番号）
  const ids = new Set()
  for (const m of html.matchAll(/race_id=(\d{12})/g)) ids.add(m[1])
  return [...ids]
}

async function fetchRaceResult(raceId) {
  const url = `https://race.netkeiba.com/race/result.html?race_id=${raceId}`
  const html = await fetchEucJp(url)
  if (!html) return null
  return parseRaceResultHtml(raceId, html)
}

function parseRaceResultHtml(raceId, html) {
  // テーブル構造（class="ResultsByRaceDetail" の RaceTable01）から
  // 着順 / 枠 / 馬番 / 馬名 / 性齢 / 斤量 / 騎手 / タイム / 着差 / 通過 / 上り / 単勝 / 人気 / 馬体重 / 厩舎
  const out = {
    raceId,
    raceName: '',
    venue: '',
    surface: '',
    distance: 0,
    grade: '通常',
    weather: null,
    trackCondition: null,
    raceNumber: null,
    startTime: null,
    className: null,
    results: [],
  }
  // ヘッダ部のパース
  const titleMatch = html.match(/<div\s+class="RaceName">([\s\S]*?)<\/div>/)
  if (titleMatch) out.raceName = stripTags(titleMatch[1]).trim()
  // RaceData01: "11:00発走 / 芝1600m (右) / 天候:晴 / 馬場:良"
  const dataMatch = html.match(/<div\s+class="RaceData01">([\s\S]*?)<\/div>/)
  if (dataMatch) {
    const txt = stripTags(dataMatch[1])
    const surfaceM = txt.match(/(芝|ダ|障)(\d{3,4})m/)
    if (surfaceM) {
      out.surface = surfaceM[1] === 'ダ' ? 'ダート' : surfaceM[1] === '芝' ? '芝' : '障害'
      out.distance = parseInt(surfaceM[2])
    }
    const weatherM = txt.match(/天候:(\S+)/)
    if (weatherM) out.weather = weatherM[1]
    const condM = txt.match(/馬場:(\S+)/)
    if (condM) out.trackCondition = condM[1]
    const startM = txt.match(/(\d{1,2}):(\d{2})発走/)
    if (startM) out.startTime = `${startM[1]}:${startM[2]}`
  }
  // RaceData02: "1回 東京 1日目 / 3歳新馬 / 16頭"
  const data2 = html.match(/<div\s+class="RaceData02">([\s\S]*?)<\/div>/)
  if (data2) {
    const venueM = data2[1].match(/(東京|中山|阪神|京都|中京|新潟|札幌|函館|小倉|福島)/)
    if (venueM) out.venue = venueM[1]
    const classM = data2[1].match(/(新馬|未勝利|1勝クラス|2勝クラス|3勝クラス|OP|オープン|L|G[123])/i)
    if (classM) {
      out.className = classM[1]
      if (/G[123]/i.test(classM[1])) out.grade = classM[1].toUpperCase()
    }
  }
  // 結果テーブル
  const tableM = html.match(/<table\s+class="RaceTable01[\s\S]*?<\/table>/)
  if (tableM) {
    const rows = tableM[0].matchAll(/<tr[^>]*>([\s\S]*?)<\/tr>/g)
    for (const row of rows) {
      const cols = [...row[1].matchAll(/<td[^>]*>([\s\S]*?)<\/td>/g)].map(c => stripTags(c[1]).trim())
      if (cols.length < 12) continue
      const finishPos = parseInt(cols[0])  // 着順 or "中止"等
      out.results.push({
        finishPosition: isNaN(finishPos) ? null : finishPos,
        scratchReason: isNaN(finishPos) ? cols[0] : null,
        frameNumber: parseInt(cols[1]) || null,
        horseNumber: parseInt(cols[2]),
        horseName: cols[3].split(/\s/)[0],
        netkeibaHorseId: extractHorseId(row[1]),
        sexAge: cols[4],
        weight: parseFloat(cols[5]) || null,
        jockey: cols[6],
        time: cols[7] || null,
        margin: cols[8] || null,
        cornerPositions: cols[9] || null,
        rapidIncrease: parseFloat(cols[10]) || null,
        odds: parseFloat(cols[11]) || null,
        popularity: parseInt(cols[12]) || null,
        horseWeight: parseHorseWeight(cols[13]),  // "480(+4)" → 480
        // weightChange は別途
      })
    }
  }
  return out
}

function parseHorseWeight(s) {
  const m = s.match(/^(\d+)\((-?\+?\d+)\)/)
  if (!m) return { hw: parseInt(s) || null, change: null }
  return { hw: parseInt(m[1]), change: parseInt(m[2].replace('+','')) }
}
```

### 2-3-4. 進捗管理

```js
const PROGRESS_FILE = path.join(__dirname, '.fetch_jra_full_progress.json')
// { lastDate: "2024-03-15", totalRaces: 12345, totalResults: 123456, errors: [...] }
```

中断時は `--resume` で続きから。

### 2-3-5. レート制限

- `SLEEP_MS = 1500` を維持（既存scriptと同じ）
- 1日あたり 36-72 レース × 1.5秒 + ログ = 約 2 分/開催日
- 5年間（260 開催日）= 約 9 時間
- 12年間（620 開催日）= 約 21 時間

夜間バックグラウンド実行を推奨。Windows なら `start /min powershell` でバックグラウンド化、進捗は `.fetch_jra_full_progress.json` で確認。

### 2-3-6. エラー処理

- HTTP 429（レート制限）→ 30秒待機 + リトライ3回
- HTTP 5xx → 5秒待機 + リトライ3回
- パース失敗 → ログに記録して次へ（中断しない）
- すべての致命的エラーは `RaceCalendar.errorLog` に保存

## 2-4. /api/reanalyze の拡張

### 2-4-1. 既存処理の確認

`src/app/api/reanalyze/route.ts` は **G1〜G3 のみ** を対象にしている可能性が高い。本書ではすべての Race（grade='通常' 含む）を対象にする。

### 2-4-2. 修正

```ts
// before
const allRaces = await prisma.race.findMany({
  where: { grade: { in: ['G1', 'G2', 'G3'] } },
  include: { results: true },
})

// after: すべてのRaceを対象（重賞外も含む）
const allRaces = await prisma.race.findMany({
  include: {
    results: { where: { finishPosition: { not: null } } },
  },
})
```

`finishPosition` が null（取消・除外）の RaceResult はスタッツ集計から除外。

### 2-4-3. パフォーマンス対策

200K+ レコードを一気に findMany すると Prisma がメモリ不足になる可能性。
**チャンク取得** に書き換え:

```ts
async function buildAllStats() {
  const allStats = new Map<string, HorseStatData>()
  const CHUNK = 5000
  let cursor: string | null = null
  while (true) {
    const races: any[] = await prisma.race.findMany({
      take: CHUNK,
      ...(cursor ? { cursor: { id: cursor }, skip: 1 } : {}),
      orderBy: { id: 'asc' },
      include: { results: { where: { finishPosition: { not: null } } } },
    })
    if (races.length === 0) break
    cursor = races[races.length - 1].id
    
    const stats = buildHorseStatsFromResults(races.map(r => ({
      name: r.name, grade: r.grade, venue: r.venue, surface: r.surface,
      distance: r.distance, date: r.date, trackCondition: r.trackCondition,
      results: r.results,
    })))
    
    for (const s of stats) {
      const ex = allStats.get(s.horseName)
      if (!ex) allStats.set(s.horseName, s)
      else mergeIntoExisting(ex, s)  // 既存の mergeStatData を活用
    }
  }
  return [...allStats.values()]
}
```

### 2-4-4. インデックス追加

`RaceResult` 件数増に伴い:

```sql
-- マイグレーションに含める
CREATE INDEX IF NOT EXISTS idx_raceresult_horsename ON "RaceResult"("horseName");
CREATE INDEX IF NOT EXISTS idx_raceresult_raceid_finish ON "RaceResult"("raceId", "finishPosition");
```

`prisma/schema.prisma` で:

```prisma
model RaceResult {
  // ...
  @@index([horseName])
  @@index([raceId, finishPosition])
}
```

## 2-5. 実行手順

```bash
# 0. マイグレーション
cd C:\keiba
npx prisma migrate dev --name v400_full_jra_schema

# 1. 直近1年（高優先度・短時間）
node scripts/fetch_jra_full_calendar.js --from=2025-05-01 --to=2026-05-13

# 2. 統計再構築（直近1年データで動作確認）
curl -X POST http://localhost:3000/api/reanalyze
curl http://localhost:3000/api/reanalyze    # horseStatCount を確認

# 3. blind 検証
node scripts/full_blind_optimize_v2.js

# 4. 過去5年（バックグラウンド可）
node scripts/fetch_jra_full_calendar.js --from=2021-01-01 --to=2025-04-30

# 5. 残り（古いデータ・非クリティカル・夜間実行）
node scripts/fetch_jra_full_calendar.js --from=2014-01-01 --to=2020-12-31

# 6. 最終 reanalyze
curl -X POST http://localhost:3000/api/reanalyze
```

## 2-6. 検証

| チェック項目                                     | 期待値                     |
| ------------------------------------------------ | -------------------------- |
| `SELECT COUNT(*) FROM "Race"`                    | 5年分 ≈ 17,000 / 12年 ≈ 40,000 |
| `SELECT COUNT(*) FROM "RaceResult"`              | 5年分 ≈ 200K / 12年 ≈ 500K |
| `SELECT COUNT(DISTINCT "horseName") FROM "HorseStat"` | 5年で 約 8,000 / 12年で 約 18,000 |
| `SELECT COUNT(*) FROM "Race" WHERE "grade" = '通常'` | 重賞外比率 80% 以上        |
| ヴェルテンベルクの HorseStat 存在確認            | totalRaces >= 3            |
| `RaceResult.weightChange` の欠損率               | 5% 以下                    |
| blind 連対率（v341 vs v400.10）                  | +3〜5pt                    |

---

# §3. Phase 2 — 推奨ロジック多軸化

## 3-1. 課題

現状 `localScoreHorses` は `placeRate` 降順 Top5 を返す（`scorer.ts` 末尾）。
連対率の **絶対値** だけで選ぶと:

- 1-3位は人気上位馬で固まる（モデルが妥当な判断）
- 4-5位は中堅人気の「無難な選択」になる
- 6人気以下の連対馬は **常に枠外**

馬連的中（推奨2頭が両方連対）するには、**1着候補1頭 + 2着候補（伏兵含む）** のセットが必要。

## 3-2. 「保守3頭 + 期待値2頭」モード

### 3-2-1. 設計

5頭の内訳を以下のように構成変更:

| 枠   | 選び方                                                   | 想定                          |
| ---- | -------------------------------------------------------- | ----------------------------- |
| 1位  | placeRate 最大                                           | 鉄板本命                      |
| 2位  | placeRate 2位                                            | 対抗                          |
| 3位  | placeRate 3位                                            | 単穴                          |
| 4位  | **期待値**最大（placeRate × oddsFloat）から1-3位を除外  | 中穴〜大穴の妙味馬           |
| 5位  | **placeRate 6-12位の中で modelScore ↔ oddsRank 乖離最大** | 市場の盲点（ヴェルテンベルク型） |

### 3-2-2. 実装

`scorer.ts` の `localScoreHorses` 末尾、`scored.sort` の **直前** で:

```ts
// 既存: スコア降順ソート + Top N 切り出し
scored.sort((a, b) => b.placeRate - a.placeRate)

// 新ロジック: 5頭の内訳構築
function selectFinalFive(scored: ScoredHorse[]): ScoredHorse[] {
  if (scored.length <= 5) return scored
  
  const sortedByRate = [...scored].sort((a, b) => b.placeRate - a.placeRate)
  const sortedByEV = [...scored].sort((a, b) => {
    const evA = (a.placeRate / 100) * (a._oddsFloat ?? 50)  // 50 は欠損時のデフォルト
    const evB = (b.placeRate / 100) * (b._oddsFloat ?? 50)
    return evB - evA
  })
  
  const picked = new Set<string>()
  const result: ScoredHorse[] = []
  
  // 上位3頭: placeRate順
  for (let i = 0; i < 3 && i < sortedByRate.length; i++) {
    result.push(sortedByRate[i])
    picked.add(sortedByRate[i].horseName)
  }
  
  // 4頭目: 未選出の中で期待値最大
  for (const h of sortedByEV) {
    if (!picked.has(h.horseName)) {
      result.push(h)
      picked.add(h.horseName)
      break
    }
  }
  
  // 5頭目: placeRate 6-12位 かつ oddsRank との乖離最大
  const candidates = sortedByRate
    .slice(5, 12)  // 予想6-12位
    .filter(h => !picked.has(h.horseName))
    .map(h => {
      const oddsRank = h._oddsRank ?? 99
      const predRank = sortedByRate.indexOf(h) + 1
      return { h, gap: predRank - oddsRank }  // 正値 = 市場が高評価
    })
    .sort((a, b) => b.gap - a.gap)
  
  if (candidates.length > 0 && candidates[0].gap >= 2) {
    result.push(candidates[0].h)
  } else {
    // 乖離なし → placeRate 4位を入れる
    for (const h of sortedByRate) {
      if (!picked.has(h.horseName)) { result.push(h); break }
    }
  }
  
  // ランクを最終配列のインデックスで再付番
  return result.map((h, i) => ({ ...h, rank: i + 1 }))
}

const finalFive = selectFinalFive(scored)
return finalFive
```

### 3-2-3. ScoredHorse 型拡張

`_oddsFloat`, `_oddsRank` を内部用に追加（外部 API 互換のため `_` プレフィックス）:

```ts
export interface ScoredHorse {
  rank: number
  horseNumber: number | null
  horseName: string
  placeRate: number
  // 追加（型上 optional だが内部では必ず設定）
  _oddsFloat?: number | null
  _oddsRank?: number | null
  _selectionReason?: 'top1' | 'top2' | 'top3' | 'expected_value' | 'market_gap' | 'fallback'
  factors: { ... }
}
```

`buildScore` の return で:

```ts
return {
  rank: 0,
  horseNumber: entry.horseNumber,
  horseName: entry.horseName,
  placeRate: ...,
  _oddsFloat: entry.oddsFloat ?? null,
  _oddsRank: entry.oddsPopularity ?? null,
  factors: { ... }
}
```

`selectFinalFive` 内で `_selectionReason` を埋める:

```ts
result[0]._selectionReason = 'top1'
result[1]._selectionReason = 'top2'
result[2]._selectionReason = 'top3'
result[3]._selectionReason = 'expected_value'
result[4]._selectionReason = candidates[0]?.gap >= 2 ? 'market_gap' : 'fallback'
```

## 3-3. UI: 選定理由の表示

`src/app/page.tsx` の予測結果カードに:

```tsx
const reasonLabel = (r?: string) => ({
  top1: '本命',
  top2: '対抗',
  top3: '単穴',
  expected_value: '期待値',
  market_gap: '市場の盲点',
  fallback: '抑え',
}[r ?? ''] ?? '')

// JSX
<span className="text-[10px] px-1.5 py-0.5 rounded bg-amber-500/20 text-amber-300">
  {reasonLabel(p._selectionReason)}
</span>
```

「市場の盲点」のチップが付く馬がヴェルテンベルク型の伏兵を担当する。

## 3-4. オン/オフ切替

ユーザーが「鉄板派」「穴党」を選べるよう、`/api/predict` の body に `mode: 'classic' | 'multiaxis'` を追加:

```ts
// predict/route.ts
const mode = body.mode ?? 'multiaxis'
// scorer に渡す
const scored = localScoreHorses(entriesWithOdds, raceWithCond, stats, weights, {
  calibration, globalIntervalBaseline: intervalBaseline,
  selectionMode: mode,
})
```

`scorer.ts` の `localScoreHorses` 末尾:

```ts
return options.selectionMode === 'classic'
  ? scored.sort((a, b) => b.placeRate - a.placeRate).slice(0, 5).map((h, i) => ({ ...h, rank: i + 1 }))
  : selectFinalFive(scored)
```

UI 側はトグルを追加（デフォルト multiaxis）。

## 3-5. 検証

過去 50 レースで `selectFinalFive` 実装前後を比較:

| 指標                    | classic | multiaxis | 期待                  |
| ----------------------- | ------- | --------- | --------------------- |
| 5頭中連対馬1頭以上の率  | 78%     | 80%+      | 維持〜微増            |
| 5頭中連対馬2頭の率      | 22%     | 30%+      | 大幅増                |
| 期待値合計（払戻 ÷ 投資） | <1.0    | >1.0      | 黒字寄り              |
| 6人気以下の連対馬カバー率 | 12%     | 35%+      | ヴェルテンベルク型救済 |

---

# §4. Phase 3 — LightGBM モデル導入（メイン精度向上）

## 4-1. 全体アーキテクチャ

```
[RaceResult 200K+] → [特徴量抽出] → [LightGBM 訓練]
                                          ↓
                                    [ONNX export]
                                          ↓
[新レース] → [特徴量生成] → [ONNX 推論] → ML確率
                                                ↓
[既存ヒューリスティック placeRate] →→→→ [アンサンブル 0.7×ML + 0.3×Heuristic] → 最終 placeRate
```

### 4-1-1. なぜ LightGBM か

- 高次交互作用を自動学習（騎手×コース×季節×馬体重）
- カテゴリ特徴量を直接扱える（`enable_categorical=True`）
- ONNX エクスポートで Node.js から軽量推論
- Python での訓練、Node での推論というハイブリッドが綺麗
- LSTM/Transformer のような GPU 不要

### 4-1-2. 必要パッケージ

訓練側（Python・別環境）:

```
lightgbm==4.3.0
scikit-learn==1.4.0
pandas==2.2.0
psycopg2-binary==2.9.9
onnxmltools==1.12.0
skl2onnx==1.16.0
```

推論側（Node.js・本プロジェクト）:

```bash
npm install onnxruntime-node@1.18.0
```

## 4-2. 特徴量設計

### 4-2-1. 訓練データのキー単位

「**1馬 × 1レース**」を1サンプル。ラベル `y = 1 if finishPosition <= 2 else 0`。
推定 200K サンプル（5年）/ 500K サンプル（12年）。

### 4-2-2. 特徴量カテゴリ

| カテゴリ                | 例                                                                     | 特徴量数 |
| ----------------------- | ---------------------------------------------------------------------- | -------- |
| (A) レース基本          | grade(cat), surface(cat), distance, venue(cat), trackCondition(cat), weather(cat), month, dayOfYear | 8 |
| (B) 馬の累積実績        | totalRaces, totalPlaces, placeRate, g1Races, g1Places, g2Places, g3Places, distanceRate@dist, venueRate@venue, surfaceRate, courseDistRate@key | 12 |
| (C) 馬の最近形          | recentForm5avg, recentForm3avg, recentTrend(slope), recentPopAvg, finishGap5avg, lastRaceDays, intervalBin(cat) | 7 |
| (D) 騎手・厩舎          | jockeyRank, jockeyVenueRate, jockeyDistRate, trainerRank, trainerRecent30Rate, trainerRecent90Rate | 6 |
| (E) 馬体重・調教        | horseWeight, weightChange, weightVsAvg, lastThreeFurlong, runningStyle(cat), seasonalWeightDiff | 6 |
| (F) 市場                | oddsPopularity, oddsFloat, oddsRank, oddsGapVsModelRank | 4 |
| (G) 血統                | sireRate, damSireRate, sireDistanceRate, damDamRate | 4 |
| (H) コース特性          | straightLength, slope(cat), direction(cat), shape(cat) | 4 |
| (I) 既存ヒューリスティック | localPlaceRate（v343の出力をそのまま入力）            | 1 |
| **合計**                |                                                                        | **52**   |

### 4-2-3. 訓練スクリプト: `ml/build_dataset.py`（新規ファイル）

```python
"""
RaceResult から LightGBM 訓練データセット (Parquet) を生成。

実行:
    python ml/build_dataset.py --from=2021-01-01 --to=2026-05-13 --out=ml/dataset.parquet
"""
import argparse
import os
import psycopg2
import pandas as pd
import json
from datetime import datetime, timedelta

DB_URL = os.environ.get('DATABASE_URL', 'postgresql://postgres:post@localhost:5432/keiba')

def fetch_results(conn, start, end):
    sql = """
    SELECT
      r.id AS race_id, r.name AS race_name, r.date, r.venue, r.grade, r.surface, r.distance,
      r."trackCondition" AS track_cond, r.weather,
      rr.id AS result_id, rr."finishPosition" AS finish_position,
      rr."horseName" AS horse_name, rr."horseNumber" AS horse_number,
      rr.age, rr.jockey, rr.trainer,
      rr.popularity AS popularity, rr.odds AS odds,
      rr."horseWeight" AS horse_weight,
      rr."rapidIncrease" AS rapid_increase,
      hs."totalRaces", hs."totalPlaces",
      hs."g1Races", hs."g1Places", hs."g2Races", hs."g2Places", hs."g3Races", hs."g3Places",
      hs."distanceData", hs."venueData", hs."surfaceData", hs."courseDistData",
      hs."intervalData", hs."recentForm", hs."recentGrades", hs."recentPops",
      hs."lastRaceDate", hs."lastRacePopularity", hs."avgHorseWeight",
      hs.sire, hs.dam, hs."sireOfSire", hs."sireOfDam", hs."damOfSire", hs."damOfDam"
    FROM "RaceResult" rr
    JOIN "Race" r ON r.id = rr."raceId"
    LEFT JOIN "HorseStat" hs ON hs."horseName" = rr."horseName"
    WHERE r.date BETWEEN %s AND %s
      AND rr."finishPosition" IS NOT NULL
    """
    cur = conn.cursor()
    cur.execute(sql, (start, end))
    cols = [c[0] for c in cur.description]
    return pd.DataFrame(cur.fetchall(), columns=cols)


def add_feature_columns(df):
    """生データから特徴量列を追加"""
    # (A) レース基本
    df['month'] = pd.to_datetime(df['date']).dt.month
    df['day_of_year'] = pd.to_datetime(df['date']).dt.dayofyear
    
    # (B) 累積実績
    df['place_rate'] = df['totalPlaces'] / df['totalRaces'].clip(lower=1)
    df['g1_rate'] = df['g1Places'] / df['g1Races'].clip(lower=1)
    
    # 距離別
    def get_dist_rate(row):
        try:
            dd = json.loads(row['distanceData']) if isinstance(row['distanceData'], str) else (row['distanceData'] or {})
            d = dd.get(str(row['distance']))
            if d and d.get('races', 0) > 0:
                return d['places'] / d['races']
        except: pass
        return None
    df['dist_rate'] = df.apply(get_dist_rate, axis=1)
    # 同様に venue/surface/course_dist の rate を生成
    
    # (C) 最近形
    def parse_form(s):
        if not s: return [None] * 5
        return [int(x) if x.isdigit() else None for x in s.split('-')[:5]]
    
    forms = df['recentForm'].apply(parse_form).apply(pd.Series)
    forms.columns = [f'form{i}' for i in range(5)]
    df = pd.concat([df, forms], axis=1)
    df['form_avg'] = forms.mean(axis=1, skipna=True)
    df['form_recent3_avg'] = forms[['form0','form1','form2']].mean(axis=1, skipna=True)
    
    # 直近からの日数
    df['days_since_last'] = (
        pd.to_datetime(df['date']) - pd.to_datetime(df['lastRaceDate'])
    ).dt.days
    
    # (E) 体重関連
    df['weight_vs_avg'] = df['horse_weight'] - df['avgHorseWeight']
    
    # (F) 市場 (オッズ rank はレース内で計算する必要がある)
    df['odds_rank'] = df.groupby('race_id')['odds'].rank(method='min')
    
    # (I) ラベル
    df['y'] = (df['finish_position'] <= 2).astype(int)
    
    return df


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument('--from', dest='start', required=True)
    parser.add_argument('--to', dest='end', required=True)
    parser.add_argument('--out', default='ml/dataset.parquet')
    args = parser.parse_args()
    
    conn = psycopg2.connect(DB_URL)
    df = fetch_results(conn, args.start, args.end)
    print(f'Fetched {len(df)} rows')
    df = add_feature_columns(df)
    
    feature_cols = [
        'grade', 'surface', 'distance', 'venue', 'track_cond', 'weather',
        'month', 'day_of_year',
        'totalRaces', 'totalPlaces', 'place_rate', 'g1Races', 'g1Places',
        'dist_rate',
        'form0', 'form1', 'form2', 'form3', 'form4', 'form_avg', 'form_recent3_avg',
        'days_since_last',
        'horse_weight', 'weight_vs_avg', 'avgHorseWeight',
        'popularity', 'odds', 'odds_rank',
        'sire', 'dam',
        # ... 全 52 特徴量
    ]
    
    out = df[feature_cols + ['y', 'race_id', 'date']]
    os.makedirs(os.path.dirname(args.out), exist_ok=True)
    out.to_parquet(args.out, index=False)
    print(f'Saved to {args.out}')


if __name__ == '__main__':
    main()
```

### 4-2-4. 訓練スクリプト: `ml/train.py`

```python
"""
LightGBM 訓練。時系列CVで早期停止。最良モデルを保存。

実行:
    python ml/train.py --dataset=ml/dataset.parquet --out-dir=ml/models
"""
import argparse
import os
import json
import pandas as pd
import lightgbm as lgb
from sklearn.metrics import roc_auc_score, log_loss
from skl2onnx import convert_sklearn
from skl2onnx.common.data_types import FloatTensorType, StringTensorType

CATEGORICAL = ['grade', 'surface', 'venue', 'track_cond', 'weather', 'sire', 'dam']

def time_split(df, valid_year=2024, test_year=2025):
    df = df.sort_values('date').reset_index(drop=True)
    train = df[pd.to_datetime(df['date']).dt.year <= valid_year - 1]
    valid = df[pd.to_datetime(df['date']).dt.year == valid_year]
    test  = df[pd.to_datetime(df['date']).dt.year >= test_year]
    return train, valid, test


def train(args):
    df = pd.read_parquet(args.dataset)
    train_df, valid_df, test_df = time_split(df)
    
    feature_cols = [c for c in df.columns if c not in ['y', 'race_id', 'date']]
    
    # カテゴリを category dtype 化
    for col in CATEGORICAL:
        if col in df.columns:
            train_df[col] = train_df[col].astype('category')
            valid_df[col] = valid_df[col].astype('category')
            test_df[col]  = test_df[col].astype('category')
    
    train_set = lgb.Dataset(
        train_df[feature_cols], label=train_df['y'],
        categorical_feature=[c for c in CATEGORICAL if c in feature_cols],
    )
    valid_set = lgb.Dataset(
        valid_df[feature_cols], label=valid_df['y'],
        reference=train_set,
        categorical_feature=[c for c in CATEGORICAL if c in feature_cols],
    )
    
    params = {
        'objective': 'binary',
        'metric': ['auc', 'binary_logloss'],
        'learning_rate': 0.05,
        'num_leaves': 63,
        'min_data_in_leaf': 200,
        'feature_fraction': 0.85,
        'bagging_fraction': 0.85,
        'bagging_freq': 5,
        'lambda_l1': 0.1,
        'lambda_l2': 0.1,
        'verbose': -1,
    }
    
    model = lgb.train(
        params, train_set,
        valid_sets=[train_set, valid_set],
        num_boost_round=2000,
        callbacks=[lgb.early_stopping(100), lgb.log_evaluation(50)],
    )
    
    # 評価
    test_pred = model.predict(test_df[feature_cols])
    test_auc = roc_auc_score(test_df['y'], test_pred)
    test_loss = log_loss(test_df['y'], test_pred)
    print(f'TEST AUC={test_auc:.4f}  LogLoss={test_loss:.4f}')
    
    # 5頭中連対馬カバー率の評価（実用 KPI）
    test_df = test_df.copy()
    test_df['pred'] = test_pred
    grouped = test_df.groupby('race_id')
    races_total = 0
    races_with_at_least_one = 0
    races_with_both = 0
    for _, g in grouped:
        if g['y'].sum() < 2: continue
        races_total += 1
        top5 = g.nlargest(5, 'pred')
        hits = top5['y'].sum()
        if hits >= 1: races_with_at_least_one += 1
        if hits >= 2: races_with_both += 1
    print(f'5頭中1+ ヒット: {races_with_at_least_one/races_total*100:.1f}%')
    print(f'5頭中2  ヒット: {races_with_both/races_total*100:.1f}%')
    
    # モデル保存
    os.makedirs(args.out_dir, exist_ok=True)
    model.save_model(os.path.join(args.out_dir, 'model.txt'))
    
    # メタデータ
    meta = {
        'feature_cols': feature_cols,
        'categorical': [c for c in CATEGORICAL if c in feature_cols],
        'best_iteration': model.best_iteration,
        'test_auc': test_auc,
        'test_loss': test_loss,
        'race5_hit1': races_with_at_least_one / races_total,
        'race5_hit2': races_with_both / races_total,
        'trained_at': pd.Timestamp.now().isoformat(),
    }
    with open(os.path.join(args.out_dir, 'meta.json'), 'w', encoding='utf-8') as f:
        json.dump(meta, f, ensure_ascii=False, indent=2)
    
    # ONNX export
    # LightGBM Booster → onnxmltools 経由
    from onnxmltools.convert.lightgbm.convert import convert as lgb_to_onnx
    from skl2onnx.common.data_types import FloatTensorType
    initial_types = [('input', FloatTensorType([None, len(feature_cols)]))]
    onnx_model = lgb_to_onnx(model, initial_types=initial_types, target_opset=15)
    with open(os.path.join(args.out_dir, 'model.onnx'), 'wb') as f:
        f.write(onnx_model.SerializeToString())
    print(f'Saved ONNX model to {args.out_dir}/model.onnx')


if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('--dataset', required=True)
    parser.add_argument('--out-dir', required=True)
    train(parser.parse_args())
```

**注意**: ONNX 化において、LightGBM のカテゴリ特徴量は完全互換でない。次の対応が必要:

- 数値以外のカテゴリ列は **訓練時に整数 ID 化**（pandas.Categorical.codes）
- メタの `feature_cols` に各カテゴリ列の `categories: [...]` も含める
- Node 推論側で同じマッピングを再現

あるいは `treelite` 経由で C ライブラリ化するという代替もあるが、**まずは数値特徴量のみで MVP を作る** のが現実的。

#### MVP の特徴量縮小案（v400.20）

LightGBM 第1版は **数値特徴量のみ 35次元** で開始:

```
totalRaces, totalPlaces, place_rate, g1Races, g1Places, g2Places, g3Places,
dist_rate, venue_rate, surface_rate, course_dist_rate,
form0, form1, form2, form3, form4, form_avg, form_recent3_avg,
days_since_last, interval_bin_id,
horse_weight, weight_vs_avg, avgHorseWeight,
popularity, odds, odds_rank, odds_log,
distance, month, day_of_year,
straight_length, slope_id,
last_three_furlong, jockey_rank, trainer_rank
```

カテゴリ列（grade, surface, venue, weather）は **One-Hot 化** または **rank統計に変換**（例: venue → venueWinRate）。

## 4-3. Node.js 推論統合

### 4-3-1. 新規モジュール: `src/lib/mlInference.ts`

```ts
import * as ort from 'onnxruntime-node'
import path from 'path'
import fs from 'fs'

const MODEL_PATH = path.join(process.cwd(), 'ml', 'models', 'model.onnx')
const META_PATH  = path.join(process.cwd(), 'ml', 'models', 'meta.json')

let session: ort.InferenceSession | null = null
let meta: { feature_cols: string[] } | null = null

export async function loadMLModel() {
  if (session && meta) return { session, meta }
  if (!fs.existsSync(MODEL_PATH)) throw new Error(`Model not found: ${MODEL_PATH}`)
  meta = JSON.parse(fs.readFileSync(META_PATH, 'utf8'))
  session = await ort.InferenceSession.create(MODEL_PATH)
  return { session, meta }
}

export interface MLFeatureInput {
  totalRaces: number
  totalPlaces: number
  placeRate: number
  g1Races: number
  g1Places: number
  // ... 全 35 次元
}

export async function predictML(features: MLFeatureInput[]): Promise<number[]> {
  const { session, meta } = await loadMLModel()
  if (!session || !meta) throw new Error('ML model not loaded')
  
  const flat = new Float32Array(features.length * meta.feature_cols.length)
  for (let i = 0; i < features.length; i++) {
    for (let j = 0; j < meta.feature_cols.length; j++) {
      const col = meta.feature_cols[j] as keyof MLFeatureInput
      flat[i * meta.feature_cols.length + j] = (features[i][col] ?? 0) as number
    }
  }
  
  const tensor = new ort.Tensor('float32', flat, [features.length, meta.feature_cols.length])
  const results = await session.run({ input: tensor })
  
  // LightGBM ONNX出力は通常 'probability' または 'output_label' / 'output_probability'
  // モデルによって異なるので最初に走らせて Object.keys(results) を確認
  const probTensor = results['probability'] ?? results['output_probability'] ?? results[Object.keys(results)[0]]
  
  // bin classification の場合、output は [N, 2] で [neg, pos]
  const data = probTensor.data as Float32Array
  const dim = data.length / features.length
  const probs: number[] = []
  for (let i = 0; i < features.length; i++) {
    if (dim === 2) probs.push(data[i * 2 + 1])  // クラス1の確率
    else probs.push(data[i])
  }
  return probs
}
```

### 4-3-2. 特徴量ビルダー: `src/lib/mlFeatures.ts`

`buildScore`/`localScoreHorses` で使うエントリと HorseStat を入力に、ML 入力ベクトルを生成:

```ts
import type { MLFeatureInput } from './mlInference'
import type { HorseStat } from '@prisma/client'

const VENUE_RATE_MAP: Record<string, number> = {
  '東京': 0.119, '中山': 0.108, '阪神': 0.115, '京都': 0.114,
  '中京': 0.111, '新潟': 0.108, '札幌': 0.110, '函館': 0.109,
  '小倉': 0.110, '福島': 0.109,
}  // 平均連対率（補助。本番ではDBから動的算出）

export function buildMLFeatures(
  entry: any,  // EntryInput
  race: any,   // RaceContext
  stat: HorseStat | null,
  intervalBaseline: Record<string, number> | null = null,
): MLFeatureInput {
  const dist = race.distance
  const distData = (stat?.distanceData as any) ?? {}
  const dKey = String(dist)
  const distRate = distData[dKey]?.races > 0
    ? distData[dKey].places / distData[dKey].races : 0.111
  const venueData = (stat?.venueData as any) ?? {}
  const venueRate = venueData[race.venue]?.races > 0
    ? venueData[race.venue].places / venueData[race.venue].races : 0.111
  // ... 他特徴量も同様に
  
  const recentForm = (stat?.recentForm ?? '').split('-').map(Number)
  
  return {
    totalRaces: stat?.totalRaces ?? 0,
    totalPlaces: stat?.totalPlaces ?? 0,
    placeRate: stat?.totalRaces ? stat.totalPlaces / stat.totalRaces : 0.111,
    g1Races: stat?.g1Races ?? 0,
    g1Places: stat?.g1Places ?? 0,
    distRate,
    venueRate,
    form0: recentForm[0] ?? 99,
    form1: recentForm[1] ?? 99,
    // ... 35次元すべて
  }
}
```

### 4-3-3. predict/route.ts 統合

`src/app/api/predict/route.ts` の **localモードブロック内** に ML 分岐を追加:

```ts
import { predictML, loadMLModel } from '@/lib/mlInference'
import { buildMLFeatures } from '@/lib/mlFeatures'
import fs from 'fs'

const ML_ENABLED = fs.existsSync('ml/models/model.onnx')
const ML_BLEND_RATIO = 0.7  // ML weight in ensemble

// ローカル予想ブロック内
if (ML_ENABLED) {
  try {
    const features = entriesWithOdds.map(e => 
      buildMLFeatures(e, raceWithCond, stats.find(s => s.horseName === e.horseName) ?? null, intervalBaseline)
    )
    const mlProbs = await predictML(features)
    
    // mlProbs (0-1) → 連対率% (0-100) スケール
    const mlPlaceRates = mlProbs.map(p => p * 100)
    
    // アンサンブル: 0.7 * ML + 0.3 * Heuristic
    scored.forEach((s, i) => {
      const heuristicRate = s.placeRate
      const mlRate = mlPlaceRates[i]
      s.placeRate = ML_BLEND_RATIO * mlRate + (1 - ML_BLEND_RATIO) * heuristicRate
      // 元の値を残しておく（factors.reason に出すため）
      ;(s as any)._mlRate = Math.round(mlRate * 10) / 10
      ;(s as any)._heuristicRate = Math.round(heuristicRate * 10) / 10
    })
    
    // 再ソート
    scored.sort((a, b) => b.placeRate - a.placeRate)
    scored.forEach((s, i) => { s.rank = i + 1 })
  } catch (err) {
    console.warn('ML inference failed, falling back to heuristic only:', err)
    // 既存ヒューリスティックのみで続行
  }
}
```

### 4-3-4. ML有効/無効フラグ

`.env` に追加:

```
ML_ENABLED=true
ML_BLEND_RATIO=0.7
```

```ts
const ML_ENABLED = process.env.ML_ENABLED === 'true' && fs.existsSync('ml/models/model.onnx')
const ML_BLEND_RATIO = parseFloat(process.env.ML_BLEND_RATIO ?? '0.7')
```

## 4-4. 訓練・デプロイ手順

```bash
# 0. Python 環境（venv 推奨）
cd C:\keiba
python -m venv ml/venv
ml\venv\Scripts\activate  # Windows
pip install -r ml/requirements.txt

# 1. データセット生成（5年分）
python ml/build_dataset.py --from=2021-01-01 --to=2026-05-13 --out=ml/dataset.parquet

# 2. 訓練（30分〜2時間）
python ml/train.py --dataset=ml/dataset.parquet --out-dir=ml/models

# 3. メタ確認
type ml\models\meta.json   # AUC が 0.78+ なら合格、0.85+ なら優秀

# 4. Node 側で動作確認
npm install onnxruntime-node@1.18.0
# 起動して /api/predict を叩き、_mlRate と _heuristicRate が両方表示されることを確認

# 5. blind 検証
node scripts/full_blind_optimize_v2.js --ml-enabled
```

`scripts/full_blind_optimize_v2.js` も ML を組み込んだ blind 評価が必要（後述 §6）。

## 4-5. ML推論失敗時のフォールバック

- ONNX ファイル不在 → ヒューリスティックのみで動作（現状互換）
- onnxruntime-node ロード失敗 → ML_ENABLED=false に降格、ログ出力
- 推論中のエラー → そのレースのみヒューリスティックで返す

## 4-6. ハイパーパラメータ調整

最初のモデルが満足な精度に達しない場合、以下を順に試す:

| 試行                     | 設定                                          | 期待効果                |
| ------------------------ | --------------------------------------------- | ----------------------- |
| 学習率を下げる           | learning_rate: 0.05 → 0.02                    | 過学習抑制              |
| 深さ拡張                 | num_leaves: 63 → 127                          | 表現力向上（過学習注意）|
| 正則化強化               | lambda_l2: 0.1 → 0.5                          | 汎化性能向上            |
| サンプリング             | bagging_fraction: 0.85 → 0.70                 | 過学習抑制              |
| 特徴量増                 | カテゴリ列のOHE追加                           | 上位 5% 精度向上        |
| ペアワイズ学習           | objective: binary → lambdarank（レース内順位学習） | レース単位最適化        |

`lambdarank` は本来「レース内の馬を順位付けする」学習なので、馬連的中率に直結する。優先的に試す価値あり。

## 4-7. アンサンブル比率の決定

```bash
# scripts/optimize_ml_blend.js を新規作成
# blend ratio を 0.0, 0.3, 0.5, 0.7, 0.9, 1.0 で振って blind 5頭ヒット率を測定
node scripts/optimize_ml_blend.js
```

最高値の ratio を `.env` に保存。

---

# §5. Phase 4 — 自動運用パイプライン

## 5-1. 週次バッチ

### 5-1-1. 月曜 02:00 JST: 結果取り込み + 自己学習

スクリプト: `scripts/weekly_update.js`

```js
// 1. 直前の週末（土・日）について fetch_jra_full_calendar を実行
// 2. /api/reanalyze を叩いて HorseStat 更新
// 3. /api/learn を叩いてアルゴリズム改善（Claude API がない場合スキップ）
// 4. blind 検証を実行し、精度劣化があればアラート
```

Windows なら **タスクスケジューラ** に登録:

```cmd
schtasks /create /tn "KeibaWeeklyUpdate" /tr "node C:\keiba\scripts\weekly_update.js" /sc weekly /d MON /st 02:00
```

### 5-1-2. 金曜 22:00 JST: 翌週末データの事前scrape

```js
// scripts/weekly_prefetch.js
// 1. 翌週末の出走表を fetch（race_id 一覧 + 出走馬）
// 2. RaceEntry を upsert（結果は来週月曜まで未確定）
// 3. オッズが確定する前夜のためライブオッズは取らない
```

### 5-1-3. 月次（毎月1日 03:00）: モデル再訓練

```cmd
schtasks /create /tn "KeibaMLRetrain" /tr "C:\keiba\ml\venv\Scripts\python.exe C:\keiba\ml\train.py --dataset=C:\keiba\ml\dataset.parquet --out-dir=C:\keiba\ml\models" /sc monthly /d 1 /st 03:00
```

データセット再生成も別タスクで:

```cmd
schtasks /create /tn "KeibaMLDataset" /tr "C:\keiba\ml\venv\Scripts\python.exe C:\keiba\ml\build_dataset.py --from=2021-01-01 --to=2026-12-31 --out=C:\keiba\ml\dataset.parquet" /sc monthly /d 1 /st 02:00
```

## 5-2. 通知

新規 `scripts/lib/notify.js`:

```js
// Windows トースト通知 or デスクトップ起動.bat 起動時に表示
function notify(title, body) {
  // PowerShell 経由でトースト
  const ps = `New-BurntToastNotification -Text "${title}", "${body}"`
  require('child_process').execSync(`powershell -Command "${ps}"`)
}
```

精度劣化アラート、ML訓練完了通知などに使用。

## 5-3. ダッシュボードページ

`src/app/admin/page.tsx`（新規）:

| セクション             | 内容                                                  |
| ---------------------- | ----------------------------------------------------- |
| 最終取り込み日         | RaceCalendar 最大日付                                 |
| 総レース数             | Race count                                            |
| 総結果数               | RaceResult count                                      |
| HorseStat 件数         | 馬名ユニーク数                                        |
| 直近 30レース 5頭ヒット率 | Prediction × RaceResult から動的算出                  |
| 直近 30レース 馬連的中率 | 同上                                                  |
| ML モデル メタ         | meta.json の AUC, log_loss, race5_hit2 を表示         |
| 重み一覧               | AlgorithmConfig の現在値                              |

---

# §6. Phase 5 — 検証フレームワーク

## 6-1. KPI 定義

5頭推奨の本質を捉える指標を **3軸** で測る:

### 6-1-1. リコール系（漏らさない）

| 指標                  | 定義                                    |
| --------------------- | --------------------------------------- |
| Hit@5(1)              | 5頭中に連対馬1頭以上含まれる確率         |
| Hit@5(2)              | 5頭中に連対馬2頭ともが含まれる確率       |
| Hit@3(1)              | 上位3頭中に連対馬1頭以上                |

### 6-1-2. プレシジョン系（精緻に）

| 指標                  | 定義                                    |
| --------------------- | --------------------------------------- |
| Top1 prec             | 1位推奨が連対する確率                   |
| Top2 prec             | 1位 AND 2位推奨が両方連対する確率       |
| Avg rank of true winners | 連対馬2頭の平均推奨順位（小さいほど良い） |

### 6-1-3. 期待値系（儲かるか）

| 指標                  | 定義                                    |
| --------------------- | --------------------------------------- |
| ROI@馬連              | 推奨上位2頭の馬連を100円買い続けた回収率 |
| ROI@3連複             | 推奨上位3頭の3連複                      |
| ROI@ワイド5点         | 5頭から2頭選ぶワイド10通り              |

## 6-2. 検証スクリプト: `scripts/eval_full.js`

```js
// 使い方: node scripts/eval_full.js --from=2025-01-01 --to=2026-04-30 [--no-ml]

// 1. 期間内の全レースをループ
// 2. 各レースについて、その時点の HorseStat スナップショット相当で predict
//    （注意: 真ブラインドにするには訓練データから当該レース日以降を除外する必要あり。
//     現時点はライブ HorseStat で簡易評価）
// 3. KPI を集計して JSON 出力 + コンソール表示
// 4. 期間別・グレード別にも集計
```

### 6-2-1. 真ブラインド版

`scripts/full_blind_optimize_v2.js` の構造を踏襲し、**RaceResult を時系列順にスキャンして HorseStat を都度再構築** する真ブラインドを維持。LightGBM の真ブラインドは **訓練を年別に複数回行う必要** があるため、中期改修として後回し可。

## 6-3. CI 化（任意）

GitHub Actions などは Vercel 完全廃止のため不要。
ローカルで `weekly_update.js` の最後に `eval_full.js` を呼んで、KPI を `.evaluation_log.json` に追記:

```js
// weekly_update.js 末尾
const evalResult = await runEval('--from=lastSunday', '--to=today')
fs.appendFileSync('.evaluation_log.json', JSON.stringify(evalResult) + '\n')
```

## 6-4. 受け入れ基準（v400 完成定義）

| Phase   | 受け入れ条件                                                               |
| ------- | -------------------------------------------------------------------------- |
| Phase 0 | 日曜にG3が表示される / 馬体重 +X が常に表示される / oddsGap blind 改善     |
| Phase 1 | RaceResult >= 200K行 / HorseStat >= 5000頭 / ヴェルテンベルクが HorseStat に存在 |
| Phase 2 | multiaxis モードで Hit@5(2) が classic 比 +5pt 以上                        |
| Phase 3 | ML AUC >= 0.78 / Hit@5(1) >= 88% / Hit@5(2) >= 30% / ROI@馬連 > 0.85       |
| Phase 4 | 月曜2時の自動取り込みが3週連続成功 / ダッシュボードが動く                  |
| Phase 5 | eval_full.js が JSON 出力 / .evaluation_log.json が継続更新                |

---

# §7. 実装スケジュール（Sonnet 用）

## 7-1. Day 1（半日: バグ修正 + データ取り込み開始）

- [ ] §1-1 races/route.ts に G3 追加
- [ ] §1-2 馬体重表示の修正
- [ ] §1-3 oddsGap 強化（scorer.ts:690 を置換、weight 1.5）
- [ ] §1-3 grid 最適化スクリプト改修
- [ ] §2-2 prisma migration v400_full_jra_schema 適用
- [ ] §2-3 fetch_jra_full_calendar.js 実装（パーサ含む）
- [ ] dry-run で 1 開催日テスト
- [ ] 直近1年バックグラウンド開始（夜間放置）

## 7-2. Day 2（データ完了 + 推奨ロジック）

- [ ] 直近1年取り込み完了確認
- [ ] §2-4 reanalyze をチャンク化に書き換え
- [ ] reanalyze 実行 → HorseStat 増殖を確認
- [ ] §3-2 selectFinalFive 実装
- [ ] §3-3 UI に選定理由チップ表示
- [ ] §3-4 mode フラグ実装
- [ ] blind 検証で classic vs multiaxis 比較

## 7-3. Day 3-4（ML パイプライン構築）

- [ ] Python 環境構築
- [ ] §4-2-3 build_dataset.py 実装
- [ ] §4-2-4 train.py 実装
- [ ] 5年データで初回訓練
- [ ] AUC 0.75+ なら次へ、未達ならハイパーパラメータ調整
- [ ] §4-3 onnxruntime-node 統合
- [ ] §4-3-3 predict/route.ts に ML 分岐追加
- [ ] _mlRate / _heuristicRate が表示されること
- [ ] §4-7 ブレンド比率最適化

## 7-4. Day 5（残データ取り込み + 自動化）

- [ ] 過去5年完了確認、12年分の取り込み開始（バックグラウンド）
- [ ] §5-1 weekly_update.js 実装
- [ ] §5-1 タスクスケジューラ登録
- [ ] §5-2 通知実装
- [ ] §5-3 admin ダッシュボード新設

## 7-5. Day 6（検証）

- [ ] §6-2 eval_full.js 実装
- [ ] 直近 50 G1 レースで全 KPI 計測
- [ ] §6-4 受け入れ基準クリアを確認
- [ ] CHANGELOG.md 更新
- [ ] git commit -m "feat: v400 全面再設計（JRA全レース取り込み + LightGBM導入 + 多軸推奨）"

## 7-6. Day 6.5（仕上げ）

- [ ] memory 更新（[[project_keiba]] と [[project_raceresult_backfill]] を最新化）
- [ ] ユーザー報告ケース（クロワデュノール&ヴェルテンベルク）で再予測 → 改善確認
- [ ] PR 作成 or main マージ

---

# §8. 実装チェックリスト（Sonnet 用詳細版）

## §8-1. Phase 0 バグ修正

```
[ ] src/app/api/races/route.ts:33  grade に 'G3' を追加
[ ] src/app/page.tsx               formatWeight ヘルパー追加 + 5頭カード表示
[ ] src/app/page.tsx               weightChange placeholder 改善
[ ] src/lib/scorer.ts:690          computeOddsGapBonus を伏兵救済対応に置換
[ ] src/lib/scorer.ts:65           DEFAULT_WEIGHTS.oddsGapMult を 1.5 に
[ ] scripts/full_blind_optimize_v2.js  oddsGapMult をグリッド対象に追加
[ ] curl /api/races で G3 が出ること
[ ] curl /api/predict で 馬体重表示確認
[ ] node scripts/full_blind_optimize_v2.js で blind 計測
[ ] git commit -m "fix: v400.5 バグ修正（G3表示・馬体重・オッズ乖離強化）"
```

## §8-2. Phase 1 データ層

```
[ ] prisma/schema.prisma  Race / RaceResult / RaceCalendar 拡張
[ ] npx prisma migrate dev --name v400_full_jra_schema
[ ] scripts/fetch_jra_full_calendar.js  新規作成
    [ ] race list parser
    [ ] race result parser (入念にテスト)
    [ ] horse weight 480(+4) パーサ
    [ ] 進捗ファイル管理
    [ ] resume / dry-run / limit
[ ] node scripts/fetch_jra_full_calendar.js --from=2026-04-01 --to=2026-04-30 --dry-run
[ ] node scripts/fetch_jra_full_calendar.js --from=2025-05-01 --to=2026-05-13
[ ] src/app/api/reanalyze/route.ts  チャンク取得に変更 + 全 grade 対象
[ ] curl POST /api/reanalyze
[ ] SELECT COUNT(*) FROM "RaceResult"  (>= 50,000 想定)
[ ] node scripts/full_blind_optimize_v2.js  blind 改善確認
[ ] バックグラウンドで残期間 fetch
[ ] git commit -m "feat: v400.10 JRA全レース取込み（カレンダー駆動）"
```

## §8-3. Phase 2 推奨ロジック

```
[ ] src/lib/scorer.ts  ScoredHorse に _oddsFloat/_oddsRank/_selectionReason 追加
[ ] src/lib/scorer.ts  selectFinalFive 関数追加
[ ] src/lib/scorer.ts  localScoreHorses で selectionMode 分岐
[ ] src/app/api/predict/route.ts  body.mode を受け取り渡す
[ ] src/app/page.tsx  選定理由チップ表示
[ ] src/app/page.tsx  classic/multiaxis トグル UI
[ ] node scripts/eval_full.js --mode=classic と --mode=multiaxis を比較
[ ] git commit -m "feat: v400.15 5頭推奨を多軸化（期待値+市場乖離）"
```

## §8-4. Phase 3 LightGBM

```
[ ] ml/venv 環境構築
[ ] ml/requirements.txt 追加
[ ] ml/build_dataset.py 実装
[ ] python ml/build_dataset.py --from=2021-01-01 --to=2026-05-13 --out=ml/dataset.parquet
[ ] ml/train.py 実装
[ ] python ml/train.py --dataset=ml/dataset.parquet --out-dir=ml/models
[ ] meta.json の AUC を確認 (>= 0.75)
[ ] AUC 不十分ならハイパラ調整
[ ] npm install onnxruntime-node@1.18.0
[ ] src/lib/mlInference.ts 実装
[ ] src/lib/mlFeatures.ts 実装
[ ] src/app/api/predict/route.ts に ML 分岐追加
[ ] .env に ML_ENABLED, ML_BLEND_RATIO 追加
[ ] curl /api/predict  → _mlRate, _heuristicRate を確認
[ ] scripts/optimize_ml_blend.js 実装 + ratio 最適化
[ ] node scripts/eval_full.js  ML 有り無し比較
[ ] git commit -m "feat: v400.20 LightGBM導入（ONNX推論）"
```

## §8-5. Phase 4 自動化

```
[ ] scripts/weekly_update.js
[ ] scripts/weekly_prefetch.js
[ ] schtasks /create x3 (週次・金曜・月次)
[ ] scripts/lib/notify.js
[ ] src/app/admin/page.tsx
[ ] git commit -m "feat: v400.25 週次自動更新と管理ダッシュボード"
```

## §8-6. Phase 5 検証

```
[ ] scripts/eval_full.js
[ ] 受け入れ基準 §6-4 すべてチェック
[ ] CHANGELOG.md
[ ] memory 更新
[ ] ユーザー実例（クロワデュノール&ヴェルテンベルク）でリプレイ
[ ] git commit -m "feat: v400 リリース（精度80%+ Hit@5(2)+15pt達成）"
```

---

# §9. リスクと対策

## 9-1. データ取込みリスク

| リスク                              | 影響           | 対策                                                |
| ----------------------------------- | -------------- | --------------------------------------------------- |
| netkeiba 規約変更で構造変動         | パーサ全壊     | dry-run で常に最新ページの先頭3レースを検証        |
| レート制限強化                      | スクレイプ停止 | sleep 1.5s 厳守、429 で 30秒待機 + 自動リトライ    |
| HTML 構造の年代差（2014 vs 2026）   | 古いレース欠損 | 段階パーサ: 新形式失敗 → 旧形式パーサにフォールバック |
| 文字コード混在                      | 文字化け       | EUC-JP 主、UTF-8 フォールバック                     |
| 開催延期/中止                       | RaceCalendar 不一致 | status=error で記録、手動再実行                  |

## 9-2. ML リスク

| リスク                              | 影響           | 対策                                                |
| ----------------------------------- | -------------- | --------------------------------------------------- |
| onnxruntime-node の Windows ビルド失敗 | ML不使用      | npm install --build-from-source 試行、ダメなら CPU-only バイナリパッケージへ切替 |
| カテゴリ列の ONNX 互換性             | 訓練エラー     | MVP は数値特徴のみ、カテゴリは One-Hot or rank化   |
| 過学習（test_auc < valid_auc -0.05） | 精度劣化      | min_data_in_leaf 引き上げ、num_leaves 削減          |
| 推論レイテンシ                      | UI 遅延        | バッチ推論（18頭まとめて）、初回 loadModel をプリウォーム |
| メタ不整合（モデル↔Node推論）       | 不正確な予測  | meta.json の hash を Node 起動時に検証              |

## 9-3. 互換性リスク

| リスク                                   | 影響                       | 対策                                          |
| ---------------------------------------- | -------------------------- | --------------------------------------------- |
| 既存 Prediction の factors._bonuses 構造変化 | 学習履歴 invalid          | _bonuses は OPTIONAL のまま、新フィールドだけ追加 |
| AlgorithmConfig の重み構造に新キー       | getLocalWeights spread 失敗 | DEFAULT_WEIGHTS と spread 順を維持             |
| seed.ts の互換性                         | dev環境破壊                 | seed は変更しない、別途 fetch_jra で増分        |
| Claude API 解約後                       | learn/verify 動作不可      | autoLearnFromNewResult 経由のみで運用、UI ボタンは disabled 表示 |

---

# §10. ロールバック方針

## 10-1. Phase 0 のみ巻き戻し

```bash
git revert <Phase 0 のコミット>
```

DB マイグレーションは互換（追加のみ）なのでスキーマロールバック不要。

## 10-2. データだけ巻き戻し

```sql
-- v400_full_jra_schema 適用前のデータ規模に戻すには:
DELETE FROM "RaceResult" WHERE "raceId" IN (
  SELECT id FROM "Race" WHERE "netkeibaRaceId" IS NOT NULL AND "createdAt" >= '2026-05-13'
);
DELETE FROM "Race" WHERE "netkeibaRaceId" IS NOT NULL AND "createdAt" >= '2026-05-13';
DELETE FROM "RaceCalendar";
```

## 10-3. ML 機能だけ無効化

```
.env: ML_ENABLED=false
```

または `ml/models/model.onnx` を削除。fallback でヒューリスティックのみ動作。

## 10-4. multiaxis モードを classic に固定

```ts
// scorer.ts localScoreHorses 末尾で固定
const finalFive = scored.sort((a, b) => b.placeRate - a.placeRate).slice(0, 5)
```

ENV 変数 `SELECTION_MODE=classic` に切替えるオプションも提供:

```ts
const mode = options.selectionMode ?? process.env.SELECTION_MODE ?? 'multiaxis'
```

---

# 付録 A: ファイル変更マトリクス

| ファイル                                       | §1  | §2  | §3  | §4  | §5  | §6  |
| ---------------------------------------------- | --- | --- | --- | --- | --- | --- |
| `prisma/schema.prisma`                         |     | ✓   |     |     |     |     |
| `src/app/api/races/route.ts`                   | ✓   |     |     |     |     |     |
| `src/app/api/predict/route.ts`                 |     |     | ✓   | ✓   |     |     |
| `src/app/api/reanalyze/route.ts`               |     | ✓   |     |     |     |     |
| `src/app/page.tsx`                             | ✓   |     | ✓   |     |     |     |
| `src/app/admin/page.tsx` (新)                  |     |     |     |     | ✓   |     |
| `src/lib/scorer.ts`                            | ✓   |     | ✓   | ✓   |     |     |
| `src/lib/mlInference.ts` (新)                  |     |     |     | ✓   |     |     |
| `src/lib/mlFeatures.ts` (新)                   |     |     |     | ✓   |     |     |
| `scripts/fetch_jra_full_calendar.js` (新)      |     | ✓   |     |     |     |     |
| `scripts/full_blind_optimize_v2.js`            | ✓   |     |     | ✓   |     | ✓   |
| `scripts/eval_full.js` (新)                    |     |     |     |     |     | ✓   |
| `scripts/optimize_ml_blend.js` (新)            |     |     |     | ✓   |     |     |
| `scripts/weekly_update.js` (新)                |     |     |     |     | ✓   |     |
| `scripts/weekly_prefetch.js` (新)              |     |     |     |     | ✓   |     |
| `scripts/lib/notify.js` (新)                   |     |     |     |     | ✓   |     |
| `ml/build_dataset.py` (新)                     |     |     |     | ✓   |     |     |
| `ml/train.py` (新)                             |     |     |     | ✓   |     |     |
| `ml/requirements.txt` (新)                     |     |     |     | ✓   |     |     |
| `.env`                                         |     |     |     | ✓   |     |     |

---

# 付録 B: 環境変数追加

`.env` および `.env.local` に追加:

```ini
# ML
ML_ENABLED=true
ML_BLEND_RATIO=0.7

# Selection Mode
SELECTION_MODE=multiaxis    # classic|multiaxis (default: multiaxis)

# Scrape rate (seconds between requests)
SCRAPE_SLEEP_MS=1500
```

---

# 付録 C: 想定 Q&A

**Q1. Claude API なしで運用できる？**
A. はい。本書のすべての機能（データ取り込み・ML 訓練・予測）は Claude 不要。`/api/learn`, `/api/verify`, `/api/reanalyze` の Claude 呼び出し部分のみ無効化される。

**Q2. ML を使わずヒューリスティック改善のみで Hit@5(2) 30% は届く？**
A. 困難。データ拡充（Phase 1）+ 多軸推奨（Phase 2）で +7-10pt は期待できるが、Hit@5(2) 30% には ML が必要。Phase 3 が本命。

**Q3. 古いデータ（2014-2017）は精度に貢献する？**
A. 限定的。LightGBM は時系列分割でテストするので古いデータは「Train のみ」となり、近年の傾向にハマらない。**重要なのは直近5年**。古いデータは「フォールバック判定（似たレース）」程度の貢献。

**Q4. 出走前のオッズ取得は必須？**
A. 強く推奨。LightGBM の特徴量で `odds`, `popularity`, `odds_rank` は **最も影響度が大きい3因子**（市場の集合知）。なくても動くが精度は -5pt 程度低下。

**Q5. Vercel に戻すなら？**
A. 本書の自動化（タスクスケジューラ、ML 訓練）は **PC ローカル前提**。Vercel に戻すなら ML 推論は Vercel Functions（onnxruntime-node 対応）、訓練はローカル/Colab で行い、ONNXファイルをデプロイするハイブリッド。Phase 4 の自動化は Vercel Cron に置換可。本書の範囲外だが、その場合は別途指示書を要する。

**Q6. 大穴狙いに振り過ぎていない？**
A. multiaxis モードは「鉄板3頭 + 期待値1頭 + 伏兵1頭」。鉄板派なら `mode=classic` で従来動作。デフォルトは multiaxis。

**Q7. テストはどこまで書く？**
A. 本書では明示的なユニットテスト指示は最小限。**統合テスト（実DB相手）** に注力。
- パーサテスト: `scripts/fetch_jra_full_calendar.js` 内に dry-run モードで実 HTML をパースし件数確認
- スコアラーテスト: `scripts/eval_full.js` の出力 KPI で回帰検知
- ML テスト: meta.json の AUC を CI 的に下限チェック

---

# 付録 D: 既存設計との依存関係マップ

```
[改修設計.md (v343, §A-§G)]
        ↓
   既存実装済み（46facbe）
        ↓
[ HorseStat 拡張: intervalData, recentPops, courseDistData, avgHorseWeight ]
        ↓
[ scorer.ts: getFinishGapBonus, getCourseDistBonus, computeOddsGapBonus ]
        ↓
        ↓ ←←← 本書 v400 はここに乗る
        ↓
[ Phase 0: バグ修正 ]                  ← 即効
        ↓
[ Phase 1: JRA 全レース取り込み ]      ← データの厚みが LightGBM の前提
        ↓
[ Phase 2: 多軸推奨（5頭の構成変更） ] ← Phase 1 のデータがあれば独立適用可
        ↓
[ Phase 3: LightGBM ]                   ← Phase 1 が済んでから
        ↓
[ Phase 4: 自動化 ]                     ← Phase 3 まで揃ってから運用
        ↓
[ Phase 5: 検証 ]                       ← 並行
```

---

以上。

実装にあたっては、**Phase 0（半日）→ Phase 1 のデータ取り込み開始（バックグラウンド）→ Phase 2 → Phase 1 完了確認 → Phase 3 → Phase 4 → Phase 5** の順序を推奨。
データ取り込みは時間がかかる（5年で約 9 時間）ため、夜間放置で並行進行させること。

各 Phase は **独立してリリース可能** なので、Phase 3 が長引いた場合でも Phase 0-2 だけで一旦リリースし、ユーザーに改善体験を提供できる。

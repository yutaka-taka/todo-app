# 連対率向上 7改修 詳細設計書（v343）

> **対象実装者**: Claude Sonnet 4.6
> **対象環境**: C:\keiba（Next.js 14 / Prisma 5 / PostgreSQL 18 / TypeScript）
> **基準コミット**: `3f69bcb`（branch: keiba）
> **目的**: 連対率（1-2着的中率）の継続的向上。現行 blind 78% → 目標 80%+。
> **共通方針**:
> - 既存の `localScoreHorses` / `buildScore`（`src/lib/scorer.ts`）に **新ファクターを追加する** か **既存ファクターを精緻化する** 形で組み込む。
> - 各改修は **DB スキーマ変更 → ビルダー（学習側） → スコアラー（予測側） → ローカル自動学習の重み登録** の順で実装する。
> - 既存スキャフォールド（`CalibrationCurve`, `JockeyVenueDistMap`, `getRestIntervalBonus`, `getWeightAbsoluteBonus`）は再利用し、内部ロジックを差し替える。
> - 各章は独立して実装・検証可能。順序は §A→§B→§E→§D→§F→§G→§C を推奨（依存少→多）。

---

## 章構成と既存コードの状況

| #   | 改修名                              | 既存スキャフォールド                                        | 新規スキーマ              | 推定実装量 |
| --- | ----------------------------------- | ----------------------------------------------------------- | ------------------------- | ---------- |
| §A  | ローテーション・間隔ビン精緻化      | `getRestIntervalBonus`（ハードコード）                      | `HorseStat.intervalData`  | 中         |
| §B  | オッズ乖離（市場の盲点検出）        | なし（`getOddsBonus` は別軸の絶対オッズ評価）               | なし（実行時計算）        | 小         |
| §C  | 騎手×コース／調教師直近30日         | `JockeyVenueDistMap`, `getJockeyVenueDistBonus`（型のみ）   | `JockeyStat`, `TrainerStat`（新規 model） | 大 |
| §D  | 前走人気と着順のギャップ            | `lastRacePopularity`, `recentForm`                          | `HorseStat.recentPops`    | 小         |
| §E  | キャリブレーション層（後段補正）    | `applyCalibration`, `getLocalCalibration`（適用側のみ完成） | なし（保存は `AlgorithmConfig.insights` 内） | 小 |
| §F  | 同コース距離複合キー実績            | `distanceData` / `venueData`（独立軸）                      | `HorseStat.courseDistData` | 中         |
| §G  | 馬体重 絶対値×変化×季節三軸評価     | `getWeightAbsoluteBonus`（基準重量未連携）                  | `HorseStat.avgHorseWeight` | 中         |

凡例: 推定実装量＝既存ファイルへの差分＋新規ファイル本数を加味した相対値（小: 半日、中: 1日、大: 2日）

---

# §A. ローテーション・間隔ビン精緻化

## A-1. 目的・期待効果

現行 `getRestIntervalBonus` は「ハードコードのレンジ判定」で、馬個体ごとの最適間隔（個性）を反映できない。
**変更方針**: 全馬集合に対する「間隔ビン別の連対率」を統計的に学習し、馬個体に対しては「同馬の過去の間隔別成績」を加点に使う二段構成にする。

期待効果: ヴェルテンベルク型（中5週ローテで好走するタイプ）の取りこぼし削減。+0.5〜1.0pt 程度。

## A-2. 間隔ビン定義

```ts
// src/lib/intervalBins.ts（新規ファイル）
export type IntervalBin =
  | 'rensen'       // 連戦（中1週: 7-13日）
  | 'standard2'    // 中2週: 14-20日
  | 'standard3'    // 中3週: 21-27日 ← ベスト帯
  | 'standard4'    // 中4週: 28-34日 ← ベスト帯
  | 'standard5'    // 中5週: 35-41日 ← ベスト帯
  | 'medium'       // 中6-8週: 42-62日
  | 'shortRest'    // 短期休養: 63-119日
  | 'midRest'      // 中期休養: 120-180日
  | 'longRest'     // 長期休養: 181日以上

export function getIntervalBin(days: number): IntervalBin {
  if (days <= 13) return 'rensen'
  if (days <= 20) return 'standard2'
  if (days <= 27) return 'standard3'
  if (days <= 34) return 'standard4'
  if (days <= 41) return 'standard5'
  if (days <= 62) return 'medium'
  if (days <= 119) return 'shortRest'
  if (days <= 180) return 'midRest'
  return 'longRest'
}
```

## A-3. DB 変更

`prisma/schema.prisma` に追加:

```prisma
model HorseStat {
  // 既存フィールドはそのまま
  intervalData Json @default("{}")  // {"standard3": {"races": 4, "places": 2}, "longRest": {...}}
}
```

`prisma migrate dev --name add_interval_data` 実行。

## A-4. ビルダー（`src/lib/horseStats.ts`）

`HorseStatData` に `intervalData: Record<string, {races, places}>` を追加。

`buildHorseStatsFromResults` のループ内で：
- 同馬の前走 `date` を `finishesMap` に保持しているので、現レース日との差分を計算
- 初回出走（前走なし）は `intervalData` に記録しない
- ビンを `getIntervalBin(days)` で判定し、`stat.intervalData[bin]` を ++

```ts
// 疑似コード（既存ループ内）
const prevFinish = (finishesMap.get(result.horseName) ?? [])
  .sort((a, b) => b.date.getTime() - a.date.getTime())[0]
if (prevFinish) {
  const days = Math.floor((race.date.getTime() - prevFinish.date.getTime()) / 86400000)
  if (days > 0) {
    const bin = getIntervalBin(days)
    if (!stat.intervalData[bin]) stat.intervalData[bin] = { races: 0, places: 0 }
    stat.intervalData[bin].races++
    if (placed) stat.intervalData[bin].places++
  }
}
```

注意: 既存ロジックではレースを日付昇順で処理しているので、`finishesMap.push` 後に **次の** レースで前走を参照できる。実装時は処理順を確認。

## A-5. スコアラー（`src/lib/scorer.ts`）

`getRestIntervalBonus` の **シグネチャは維持**しつつ、内部実装を二段ロジックに置換:

```ts
// 全馬統計（フィールド全体の傾向）。AlgorithmConfig.insights 内に保存。
// 形式: { rensen: 0.18, standard3: 0.32, ... }（連対率）
// 学習側で四半期ごとに再計算。スコアラー側はキャッシュ読み込み。
let GLOBAL_INTERVAL_BASELINE: Record<IntervalBin, number> | null = null

export function setGlobalIntervalBaseline(b: Record<string, number> | null) {
  GLOBAL_INTERVAL_BASELINE = (b as any) ?? null
}

function getRestIntervalBonus(
  stat: HorseStat & { lastRacePopularity?: number | null; intervalData?: any },
  raceDate: Date | undefined,
): number {
  if (!stat.lastRaceDate || !raceDate) return 0
  const days = Math.floor((new Date(raceDate).getTime() - new Date(stat.lastRaceDate).getTime()) / 86400000)
  if (days <= 0) return 0
  const bin = getIntervalBin(days)

  // ① 馬個体の同ビン実績
  const personalData = (stat.intervalData ?? {}) as Record<string, { races: number; places: number }>
  const own = personalData[bin]
  let personalBonus = 0
  if (own && own.races >= 2) {
    const rate = own.places / own.races
    const sf = Math.min(own.races, 5) / 5
    if (rate >= 0.50) personalBonus = Math.round(8 * sf)
    else if (rate >= 0.30) personalBonus = Math.round(3 * sf)
    else if (rate < 0.10) personalBonus = -Math.round(4 * sf)
  }

  // ② 全馬基準（ビン別の総合傾向）
  let globalBonus = 0
  if (GLOBAL_INTERVAL_BASELINE) {
    const r = GLOBAL_INTERVAL_BASELINE[bin]
    if (r != null) {
      const overallAvg = 0.111  // 18頭中2頭 ≈ 11.1%
      globalBonus = Math.round((r - overallAvg) * 60)  // ±6pt 程度に収まる
    }
  }

  // ③ 既存「叩き良化」判定は残す（先行実装と整合）
  const wasLikelyPrep = false  // 既存ロジックを再利用（紙面省略）
  const isTakiType = false     // 同上

  // ④ 個体>全体>叩きヒューリスティクス の優先順
  if (personalBonus !== 0) return personalBonus
  if (globalBonus !== 0) return globalBonus
  // 叩き判定の既存処理を残してフォールバック…
  return /* 既存実装 */ 0
}
```

## A-6. 全馬基準の集計

`/api/reanalyze` に追加処理を入れる:

```ts
// すべての RaceResult から間隔ビン別連対率を集計
async function computeGlobalIntervalBaseline(): Promise<Record<string, number>> {
  // 馬名 → 着順履歴を date 昇順で構築
  // 各レースで「前走からの日数→ビン→連対判定」を集計
  // 最終的に bin ごとの places/races を計算して連対率の Record を返す
}

// AlgorithmConfig.insights（JSON） に保存:
// { ..., globalIntervalBaseline: {...} }
```

予測時 (`/api/predict` または `localScoreHorses` 呼び出し直前) に `setGlobalIntervalBaseline()` を呼んで反映。

## A-7. 重みと自己学習

新ファクター `restIntervalMult` は **既存** （DEFAULT_WEIGHTS にある）。
内部ロジックの変更だけなので、重みキーの追加は不要。
学習側は `_bonuses.restInterval` を引き続き記録して `restIntervalMult` を調整する。

## A-8. 検証

- 同一テストレース（直近 G1 5レース）を旧実装と新実装で予測し、Top5 と連対馬の重なりを比較
- backfill 後の HorseStat に対して `intervalData` が想定どおり生成されているか SQL で抽出: `SELECT horseName, intervalData FROM "HorseStat" WHERE intervalData::text != '{}' LIMIT 20;`

---

# §B. オッズ乖離（市場の盲点検出）

## B-1. 目的・期待効果

現行 `getOddsBonus` は単独馬のオッズ絶対値で加点。市場と予想の **相対乖離** は使えていない。

**変更方針**: 全馬を初回スコアリング → スコア順位 (`predRank`) と オッズ順位 (`oddsRank`) を比較 → 乖離量に応じて二回目の補正を加える。

期待効果: ヴェルテンベルク型（市場6人気スコア4位 → 連対）系の伏兵を底上げ。+0.5〜1.5pt。

## B-2. 実装位置

`src/lib/scorer.ts` の `localScoreHorses` 内、最終ランキング確定の **直前** （softmax 後、cap 適用前）に乖離補正フェーズを挟む。

```ts
// localScoreHorses 内（既存の softmax ブロックの直後、scored.sort の前）

// オッズ順位を全馬に付与
const oddsList = entries
  .map((e, i) => ({
    name: e.horseName,
    oddsFloat: e.oddsFloat ?? null,
    oddsPop: e.oddsPopularity ?? null,
  }))
  .filter((x) => x.oddsFloat != null || x.oddsPop != null)

let oddsRankMap: Map<string, number> | null = null
if (oddsList.length >= entries.length * 0.7) {
  // 70%以上で順位付け可能な場合のみ実行
  const sorted = oddsList
    .slice()
    .sort((a, b) => {
      // oddsFloat があれば優先、なければ人気順位
      const av = a.oddsFloat ?? (a.oddsPop ?? 99) * 10
      const bv = b.oddsFloat ?? (b.oddsPop ?? 99) * 10
      return av - bv
    })
  oddsRankMap = new Map(sorted.map((x, i) => [x.name, i + 1]))
}

// 予想順位を一時付与
const tempSorted = scored.slice().sort((a, b) => b.placeRate - a.placeRate)
const predRankMap = new Map(tempSorted.map((s, i) => [s.horseName, i + 1]))

// 乖離補正
if (oddsRankMap) {
  for (const s of scored) {
    const pr = predRankMap.get(s.horseName)
    const or = oddsRankMap.get(s.horseName)
    if (pr == null || or == null) continue
    const gap = or - pr  // 正: スコア > 市場（市場が見落とし）／負: 市場 > スコア（過剰人気）
    const bonus = computeOddsGapBonus(gap, pr, weights.oddsGapMult ?? 1.0)
    s.placeRate += bonus
    if (s.factors._bonuses) s.factors._bonuses.oddsGap = bonus
  }
}
```

## B-3. 補正関数

```ts
// 「市場とスコアの乖離」を補正値に変換
// pr: 予想順位（1=最上位）
// gap = oddsRank - predRank
//   gap >= +3: 市場が見落としている（買い増し候補）
//   gap <= -3: 市場が過剰評価（売り候補）
function computeOddsGapBonus(gap: number, predRank: number, weightMult: number): number {
  // 上位5頭以外には適用しない（下位の乖離はノイズ）
  if (predRank > 5) return 0
  let raw = 0
  if (gap >= 5)       raw = 4
  else if (gap >= 3)  raw = 2
  else if (gap <= -5) raw = -3
  else if (gap <= -3) raw = -1
  return Math.round(raw * weightMult)
}
```

## B-4. 重みと自己学習

`LocalWeights` に `oddsGapMult: number` を追加（DEFAULT_WEIGHTS で 1.0）。
`_bonuses.oddsGap` を `factors` に追加。
`localAutoLearn.computeFactorAccuracy` の `factorMap` に `['oddsGapMult', bonuses.oddsGap ?? 0]` を追加。

## B-5. 検証

- 過去 50 レースで「予想 Top5 だが市場 6人気以下」だった馬の連対率を抽出
- その層に +2pt 加算したときの Top2 連対的中率の変化をログ出力

---

# §C. 騎手×コース／調教師直近30日

## C-1. 目的・期待効果

`JockeyVenueDistMap` の型と適用関数 (`getJockeyVenueDistBonus`) は既に scorer 側にあるが、
**ビルダー（生成側）** と **DB 永続化** が未実装。
**追加で**、調教師の直近30日好調度を別軸で評価する。

期待効果: 重賞特有の「乗り替わりで化ける」「絶好調厩舎」パターンの捕捉。+0.3〜0.8pt。

## C-2. DB 変更

新規モデル 2 つ:

```prisma
// 騎手×場×距離グループ別成績
model JockeyStat {
  id        String  @id @default(cuid())
  jockey    String
  venue     String
  distGroup String  // 'short' | 'mile' | 'middle' | 'long'
  races     Int     @default(0)
  places    Int     @default(0)
  updatedAt DateTime @updatedAt
  @@unique([jockey, venue, distGroup])
  @@index([jockey])
}

// 調教師の直近30日成績（rolling window）
model TrainerStat {
  id            String  @id @default(cuid())
  trainer       String  @unique
  recent30Races Int     @default(0)
  recent30Places Int    @default(0)
  recent90Races Int     @default(0)
  recent90Places Int    @default(0)
  computedAt    DateTime @default(now())
  updatedAt     DateTime @updatedAt
}
```

`prisma migrate dev --name add_jockey_trainer_stats`。

## C-3. ビルダー

`src/lib/jockeyTrainerStats.ts`（新規）:

```ts
export async function rebuildJockeyStats(prisma: PrismaClient) {
  // すべての RaceResult を走査
  // (jockey, venue, distGroup) ごとに集計
  // upsert で JockeyStat に保存
}

export async function rebuildTrainerStats(prisma: PrismaClient, refDate: Date) {
  // refDate から 30日 / 90日 さかのぼる
  // 各 trainer の RaceResult を集計
  // races, places を更新
}
```

`/api/reanalyze` 末尾に呼び出しを追加（既存の HorseStat 再構築の直後）。

## C-4. スコアラーへの引き渡し

`/api/predict` で:

```ts
const [stats, weights, calibration, jvdMap, trainerMap] = await Promise.all([
  prisma.horseStat.findMany({ where: { horseName: { in: horseNames } } }),
  getLocalWeights(race.grade),
  getLocalCalibration(),
  loadJockeyVenueDistMap(prisma),
  loadTrainerRecentMap(prisma),
])

const scored = localScoreHorses(entriesWithOdds, raceWithCond, stats, weights, {
  calibration,
  jockeyVenueDistMap: jvdMap,
  trainerRecentMap: trainerMap,  // ← 新規追加
})
```

## C-5. 調教師直近30日ボーナス

```ts
// scorer.ts に追加
export type TrainerRecentMap = Map<string, { r30: number; p30: number; r90: number; p90: number }>

function getTrainerRecentBonus(
  trainer: string | null | undefined,
  map: TrainerRecentMap | null | undefined,
): number {
  if (!trainer || !map) return 0
  const s = map.get(trainer)
  if (!s) return 0
  // 30日: 5走以上で連対率算出
  if (s.r30 >= 5) {
    const rate = s.p30 / s.r30
    if (rate >= 0.40) return 5  // 絶好調
    if (rate >= 0.30) return 3
    if (rate < 0.10 && s.r30 >= 8) return -3  // 不調
  }
  // 30日が薄ければ 90日
  if (s.r90 >= 10) {
    const rate = s.p90 / s.r90
    if (rate >= 0.35) return 2
    if (rate < 0.10) return -1
  }
  return 0
}
```

`buildScore` 内、`partialBonus` ／ `totalBonus` 加算箇所（`trainerMult` 適用直後）に組み込み:

```ts
const trainerRecentRaw = getTrainerRecentBonus(entry.trainer, options.trainerRecentMap)
const trainerRecentBonus = Math.round(trainerRecentRaw * (weights.trainerRecentMult ?? 1.0))
totalBonus += trainerRecentBonus
if (_bonuses) _bonuses.trainerRecent = trainerRecentBonus
```

## C-6. 重み

`LocalWeights` に追加:

```ts
trainerRecentMult: number  // DEFAULT 1.0
// jockeyVenueDistMult は既存
```

`localAutoLearn` の factorMap に追加。

## C-7. 検証

- `JockeyStat` テーブルが 100 件以上生成されること
- 直近30日に 5走以上ある trainer 数を確認（10 件以上あれば実用域）
- 騎手×場のキー欠落時に `getJockeyVenueDistBonus` がゼロ返しすること

---

# §D. 前走人気と着順のギャップ

## D-1. 目的・期待効果

`recentForm = "1-2-1-3-2"`（着順）と `lastRacePopularity` （単一値）は既に保存しているが、
**「人気と着順の乖離」は計算していない**。
たとえば「1番人気で4着」は失速、「8番人気で3着」は健闘。後者に微加点したい。

期待効果: +0.3〜0.7pt。実装が軽い割に効きやすい。

## D-2. DB 変更

```prisma
model HorseStat {
  // 既存
  recentPops    String?  // "1-3-8-2-5"（recentForm と同じ並び順の人気）
}
```

## D-3. ビルダー

`src/lib/horseStats.ts` の `finishesMap` を拡張済み（`popularity` を保持）なので、
`recentGrades` と同じパターンで `recentPops` を生成:

```ts
finishesMap.forEach((finishes, horseName) => {
  // 既存
  finishes.sort((a, b) => b.date.getTime() - a.date.getTime())
  const recent = finishes.slice(0, 5)
  stat.recentForm = recent.map((f) => f.position).join('-')
  stat.recentGrades = recent.map((f) => f.grade).join('-')
  stat.recentPops = recent.map((f) => f.popularity ?? 0).join('-')  // 新規
})
```

`/api/learn` と `/api/reanalyze` の `recentGrades` を保存している箇所すべてに `recentPops` も追加。

## D-4. スコアラー

`buildScore` 内、`recentForm` 評価の直後にギャップ補正を追加:

```ts
function getFinishGapBonus(recentForm?: string | null, recentPops?: string | null): number {
  if (!recentForm || !recentPops) return 0
  const positions = recentForm.split('-').map(Number).filter((n) => !isNaN(n) && n > 0)
  const pops      = recentPops.split('-').map(Number).filter((n) => !isNaN(n))
  if (positions.length === 0 || pops.length === 0) return 0

  // 直近3走に絞る（古い情報は信頼性低）
  const window = Math.min(3, positions.length, pops.length)
  let bonus = 0
  for (let i = 0; i < window; i++) {
    const pos = positions[i]
    const pop = pops[i]
    if (pop === 0) continue
    const gap = pos - pop  // 負: 健闘 / 正: 失速
    const recencyMult = i === 0 ? 1.0 : i === 1 ? 0.5 : 0.25
    if (gap <= -3 && pos <= 3)         bonus += 4 * recencyMult  // 大健闘
    else if (gap <= -2 && pos <= 5)    bonus += 2 * recencyMult
    else if (gap >= 3 && pop <= 2)     bonus -= 4 * recencyMult  // 人気馬大敗
    else if (gap >= 2 && pop <= 3)     bonus -= 2 * recencyMult
  }
  return Math.round(Math.max(-8, Math.min(8, bonus)))
}
```

`buildScore` 内（`recentForm` ボーナス計算の直後）:

```ts
const finishGapRaw = getFinishGapBonus(stat.recentForm, (stat as any).recentPops)
const finishGapBonus = Math.round(finishGapRaw * (weights.finishGapMult ?? 1.0))
totalBonus += finishGapBonus
if (_bonuses) _bonuses.finishGap = finishGapBonus
```

## D-5. 重み

`LocalWeights.finishGapMult: number`（DEFAULT 1.0）。`localAutoLearn` factorMap に追加。

## D-6. 検証

- HorseStat に `recentPops` が `recentForm` と同じ要素数で生成されているか
- ヴェルテンベルク（仮: 前走 8人気3着）のような馬で `finishGap` ボーナスが正値になることを `_bonuses` から確認

---

# §E. キャリブレーション層（後段補正）

## E-1. 目的・期待効果

scorer に `applyCalibration` と `getLocalCalibration` は実装済みだが、
**カーブを学習して保存する処理が未実装**。常に `{ points: [] }`（無効）。

**変更方針**: 直近100レースの `(predictedPlaceRate, actualPlaced)` ペアからスコア帯別実連対率を集計し、Isotonic-like な単調曲線を作って `AlgorithmConfig.insights.calibration` に保存。

期待効果: 上位帯（70%超）の予測過大表示を是正し、現実の連対率に校正。直接的な精度効果よりも、UI 表示の信頼性と自己学習ループの安定化に効く。+0.3〜0.5pt。

## E-2. ビン定義

```ts
// 5pt 刻み: 20-25, 25-30, ..., 65-70, 70+
const BINS = [
  [0, 25], [25, 30], [30, 35], [35, 40], [40, 45],
  [45, 50], [50, 55], [55, 60], [60, 65], [65, 100],
]
```

## E-3. 学習関数

`src/lib/localAutoLearn.ts` に追加:

```ts
export async function rebuildCalibrationCurve(): Promise<{ points: { pred: number; actual: number }[] }> {
  // 直近 100 レースの Prediction × RaceResult を取得
  const races = await prisma.race.findMany({
    where: { predictions: { some: {} }, results: { some: {} } },
    include: { predictions: true, results: { take: 2, orderBy: { finishPosition: 'asc' } } },
    orderBy: { date: 'desc' },
    take: 100,
  })

  type Bin = { pred: number; actualHits: number; total: number }
  const bins: Bin[] = BINS.map(([lo, hi]) => ({ pred: (lo + hi) / 2, actualHits: 0, total: 0 }))

  for (const r of races) {
    const top2 = new Set(r.results.filter(x => x.finishPosition <= 2).map(x => x.horseName))
    if (top2.size < 2) continue
    for (const p of r.predictions) {
      const i = BINS.findIndex(([lo, hi]) => p.placeRate >= lo && p.placeRate < hi)
      if (i < 0) continue
      bins[i].total++
      if (top2.has(p.horseName)) bins[i].actualHits++
    }
  }

  // 単調増加を保証する pool-adjacent-violators の簡易版
  const points = bins
    .filter(b => b.total >= 5)
    .map(b => ({ pred: b.pred, actual: (b.actualHits / b.total) * 100 }))

  // 単調化: 後ろ向き走査で前項より小さければ平均化
  for (let i = points.length - 1; i > 0; i--) {
    if (points[i].actual < points[i - 1].actual) {
      const avg = (points[i].actual + points[i - 1].actual) / 2
      points[i].actual = avg
      points[i - 1].actual = avg
    }
  }

  return { points }
}
```

## E-4. 保存と読み込み

- 既存 `getLocalCalibration` は `AlgorithmConfig.insights.calibration` を読む（既存実装維持）
- 新規 `saveLocalCalibration(curve)` を追加し、`/api/reanalyze` の末尾、および `autoLearnFromNewResult` 後（10レース以上の新着あり時）に呼び出す
- 保存形式: `insights` JSON に `{ ..., calibration: { points: [...] } }` を merge

## E-5. 検証

- `/api/reanalyze` 実行後、`SELECT insights FROM "AlgorithmConfig" WHERE "isActive" = true` で `calibration.points.length >= 3` を確認
- 予想実行 → 上位馬の `placeRate` が校正前後で 5-15pt 程度抑制されることを確認

## E-6. 注意

- `points.length < 2` のときは `applyCalibration` が原値返しなので副作用なし
- 校正は **最後** に適用される（上限 cap・隣接調整の後）。順序維持。

---

# §F. 同コース距離複合キー実績

## F-1. 目的・期待効果

`distanceData` と `venueData` を独立に持っているが、「東京芝2400m」のような複合条件での得意度は集計できていない。
重賞では同条件巧者が顕著（例: 東京芝2400 = ジャパンカップと日本ダービー両方の舞台）。

期待効果: +0.2〜0.6pt。

## F-2. DB 変更

```prisma
model HorseStat {
  courseDistData Json @default("{}")  // {"東京-芝-2400": {"races": 3, "places": 2}}
}
```

## F-3. ビルダー

`buildHorseStatsFromResults` 内、distanceData / venueData の隣で：

```ts
const cdKey = `${race.venue}-${race.surface}-${race.distance}`
if (!stat.courseDistData[cdKey]) stat.courseDistData[cdKey] = { races: 0, places: 0 }
stat.courseDistData[cdKey].races++
if (placed) stat.courseDistData[cdKey].places++
```

`/api/learn` と `/api/reanalyze` の `mergeStatData` 呼び出し箇所に `courseDistData` を追加。

## F-4. スコアラー

`buildScore` 内、既存の `distanceMult` / `venueMult` の **後段** で複合キー加点を追加:

```ts
function getCourseDistBonus(
  cdData: StatRecord,
  venue: string,
  surface: string,
  distance: number,
): number {
  const key = `${venue}-${surface}-${distance}`
  const d = cdData[key]
  if (!d || d.races < 2) return 0
  const rate = d.places / d.races
  const sf = Math.min(d.races, 5) / 5
  if (rate >= 0.50) return Math.round(10 * sf)
  if (rate >= 0.30) return Math.round(5 * sf)
  if (rate < 0.10 && d.races >= 4) return -Math.round(4 * sf)
  return 0
}

// 適用箇所
const cdData = (stat as any).courseDistData as StatRecord ?? {}
const cdRaw = getCourseDistBonus(cdData, race.venue, race.surface, race.distance)
const cdBonus = Math.round(cdRaw * (weights.courseDistMult ?? 1.0))
totalBonus += cdBonus
if (_bonuses) _bonuses.courseDist = cdBonus
```

## F-5. 重み

`LocalWeights.courseDistMult: number`（DEFAULT 1.0）。`localAutoLearn` factorMap 追加。

## F-6. 注意

- distanceData / venueData は維持（独立軸の弱い情報も使う）
- 複合キーは「合致したときだけ強い加点」のロジックなので、合致なしで原点を引かない

## F-7. 検証

- backfill 後、`SELECT horseName, courseDistData FROM "HorseStat" WHERE jsonb_array_length(jsonb_object_keys(courseDistData)::jsonb) > 0 LIMIT 10;` で生成確認
- 同条件で複勝率 100% の馬（小サンプル）に過剰加点されないこと（sf で抑制）

---

# §G. 馬体重 絶対値×変化×季節 三軸評価

## G-1. 目的・期待効果

`getWeightAbsoluteBonus` は既に scorer に存在するが、
**`referenceWeight`（馬の基準体重）が外部から渡ってこない** ため有効化できていない。
+ 季節（夏: 6-9月、冬: 12-2月）で適正帯が変動する点を未考慮。

期待効果: +0.2〜0.5pt。

## G-2. DB 変更

```prisma
model HorseStat {
  avgHorseWeight  Float?  // 過去全レースの平均出走時体重
  weightSamples   Int     @default(0)  // サンプル数（信頼度判定用）
}
```

## G-3. ビルダー

`buildHorseStatsFromResults` で、`RaceResult.horseWeight` を集計:

```ts
// 馬名 → 体重サンプル配列
const weightsMap = new Map<string, number[]>()

// ループ内
if (result.horseWeight && result.horseWeight > 300 && result.horseWeight < 700) {
  if (!weightsMap.has(result.horseName)) weightsMap.set(result.horseName, [])
  weightsMap.get(result.horseName)!.push(result.horseWeight)
}

// 集計後
weightsMap.forEach((arr, name) => {
  const stat = statsMap.get(name)
  if (stat && arr.length > 0) {
    stat.avgHorseWeight = arr.reduce((a, b) => a + b, 0) / arr.length
    stat.weightSamples = arr.length
  }
})
```

注意: `RaceResult.horseWeight` が backfill されていない場合は値が入らない。
現状の RaceResult backfill 状況を確認し、必要なら別途 backfill タスクを切る。

## G-4. スコアラーへの連結

`buildScore` 内、既存 `getWeightAbsoluteBonus` 呼び出し箇所:

```ts
// 既存（しかし referenceWeight が常に null）
const weightAbsRaw = getWeightAbsoluteBonus(
  entry.horseWeight,
  (stat as any).avgHorseWeight ?? null,  // ← ここを HorseStat から渡すよう修正
  race.surface,
)
```

## G-5. 季節補正の追加

`getWeightAbsoluteBonus` のシグネチャを拡張、または別関数を新設:

```ts
function getSeasonalWeightBonus(
  currentWeight: number | null,
  avgWeight: number | null,
  raceDate: Date | undefined,
): number {
  if (!currentWeight || !avgWeight || !raceDate) return 0
  const diff = currentWeight - avgWeight
  const m = raceDate.getMonth() + 1
  const isSummer = m >= 6 && m <= 9
  const isWinter = m === 12 || m === 1 || m === 2

  // 夏は -2〜0kg、冬は 0〜+4kg が好走帯
  if (isSummer) {
    if (diff >= -2 && diff <= 1) return 2
    if (diff > 4) return -2
    if (diff < -6) return -2
  } else if (isWinter) {
    if (diff >= 0 && diff <= 4) return 2
    if (diff < -3) return -2
    if (diff > 8) return -2
  }
  return 0
}
```

`buildScore` で `weightAbsBonus` の **隣に** 加える。`weightAbsMult` で同重みにする。

## G-6. 重み

既存 `weightAbsMult` を流用（季節補正は同チャンネルで計上）。
`_bonuses.weightAbs` に統合カウントすれば学習側の変更不要。

## G-7. 検証

- `SELECT COUNT(*) FROM "HorseStat" WHERE "avgHorseWeight" IS NOT NULL` が 500 以上
- 6月のレース予想で実体重 -2kg〜+1kg の馬に +2pt が乗ること

---

# §H. ローカル自動学習への組込み（横断改修）

7改修ぶんの新ファクター/重み追加に対する横断作業:

## H-1. `LocalWeights` 最終形

```ts
export interface LocalWeights {
  // 既存（変更なし）
  recentFormMult, distanceMult, venueMult, surfaceMult, g1Mult,
  ageMult, jockeyMult, raceAffinityMult, trackCondMult,
  gateMult, trainerMult, lastThreeFurlongMult, restIntervalMult,
  courseFeatureMult, paceMult, oddsMult, bloodlineMult,
  weightAbsMult, jockeyVenueDistMult,
  // 新規
  oddsGapMult: number          // §B
  trainerRecentMult: number    // §C
  finishGapMult: number        // §D
  courseDistMult: number       // §F
}
```

`DEFAULT_WEIGHTS` の新規キーはすべて 1.0 で初期化。

## H-2. `_bonuses` の最終形

```ts
_bonuses?: {
  // 既存
  recentForm, distance, venue, surface, g1, age, jockey, raceAffinity, trackCond,
  prep, gate, trainer, lastThreeFurlong, restInterval, courseFeature, pace,
  odds, bloodline, weightAbs?, jockeyVenueDist?,
  // 新規
  oddsGap?: number          // §B
  trainerRecent?: number    // §C
  finishGap?: number        // §D
  courseDist?: number       // §F
}
```

## H-3. `computeFactorAccuracy` の `factorMap` に追加

```ts
['oddsGapMult',       bonuses.oddsGap       ?? 0],
['trainerRecentMult', bonuses.trainerRecent ?? 0],
['finishGapMult',     bonuses.finishGap     ?? 0],
['courseDistMult',    bonuses.courseDist    ?? 0],
```

## H-4. `getLocalWeights` の defaults merge

`gradeWeights.G1` / `G2G3` 適用箇所では新キーがあっても DEFAULT_WEIGHTS から fallback されるよう、spread 順を `{ ...DEFAULT_WEIGHTS, ...persistedWeights }` の形で確認。

---

# §I. データ再構築フロー（必須実行手順）

新スキーマフィールドが入っただけでは過去の HorseStat には反映されない。
以下を **§A-§G の実装完了後に一括実行** すること。

```
1. prisma migrate dev --name v343_accuracy_features
   → §A intervalData, §C JockeyStat / TrainerStat, §D recentPops,
     §F courseDistData, §G avgHorseWeight, weightSamples を一括追加
2. npm run build                ← 型エラーゼロ確認
3. POST /api/reanalyze          ← 全 HorseStat を再構築（intervalData / recentPops / courseDistData / avgHorseWeight 反映）
4. GET  /api/reanalyze          ← horseStatCount, version, accuracy を確認
5. POST /api/predict （直近 G1）← _bonuses に新キーがあるか確認
6. POST /api/learn  （±0 でも一度実行）
   → 自己学習がエラーなく完走することを確認
```

---

# §J. 検証計画（全体）

## J-1. ユニット相当のチェック

- 各章 `_bonuses` フィールドに新キーがレスポンスに現れるか
- DB 集計フィールドが空 `{}` のまま放置されないか（§A intervalData, §F courseDistData は最低 1 馬で非空）

## J-2. 回帰テスト

- 直近 30 レースに対し旧スコアラー vs 新スコアラーの Top5 を出力し、入れ替わりが 30% を超えないこと（過剰変動防止）
- 連対馬の Top5 in 率の比較: 改善 +1pt 以上を期待

## J-3. blind 検証

`scripts/blind_evaluation.ts` 等で、過去 G1 50レースに対し:

- 旧 (3f69bcb): blind 連対率 = 78%
- 新 (v343):    blind 連対率 ≥ 79.5% を目標

下回った場合は新ファクターの重みを 0.5 など低めに設定して再評価し、自己学習で最適化される余地を残す。

## J-4. パフォーマンス

- `/api/predict` の応答時間が 2 秒以内（オッズ取得を除く）
- HorseStat 単一クエリのサイズ増加（recentPops, intervalData, courseDistData, avgHorseWeight 追加分）が 5KB/馬以下

---

# §K. ロールバック方針

- すべての新規フィールドは `@default("{}")` または `nullable` で導入。既存データに対する破壊的変更なし。
- スコアラー側の新ボーナスはすべて `weights.xxxMult ?? 1.0` でガード済みのため、`DEFAULT_WEIGHTS` から該当キーを 0 にすれば実質無効化できる。
- 緊急時: 旧スコアラーへ戻すには `git revert` で十分（DB マイグレーションは互換）。

---

# §L. 実装チェックリスト（Sonnet 用）

```
[ ] §A 間隔ビンビルダー
    [ ] src/lib/intervalBins.ts 新設
    [ ] HorseStat.intervalData 追加（migrate）
    [ ] horseStats.ts でビン集計
    [ ] /api/learn /api/reanalyze で intervalData merge
    [ ] scorer.ts getRestIntervalBonus を二段ロジックに置換
    [ ] AlgorithmConfig.insights.globalIntervalBaseline の保存・読込
[ ] §B オッズ乖離
    [ ] LocalWeights.oddsGapMult, _bonuses.oddsGap 追加
    [ ] localScoreHorses 内で乖離補正フェーズ追加
    [ ] localAutoLearn factorMap 追加
[ ] §C 騎手×コース／調教師直近
    [ ] JockeyStat / TrainerStat model 追加（migrate）
    [ ] src/lib/jockeyTrainerStats.ts 新設（rebuild 関数）
    [ ] /api/reanalyze 末尾で rebuild 実行
    [ ] /api/predict で jvdMap / trainerMap をロードしオプション渡し
    [ ] scorer に getTrainerRecentBonus 追加
    [ ] LocalWeights.trainerRecentMult, _bonuses.trainerRecent 追加
[ ] §D 前走人気ギャップ
    [ ] HorseStat.recentPops 追加（migrate）
    [ ] horseStats.ts で recentPops 生成
    [ ] /api/learn /api/reanalyze に recentPops 保存処理追加
    [ ] scorer に getFinishGapBonus 追加
    [ ] LocalWeights.finishGapMult, _bonuses.finishGap 追加
[ ] §E キャリブレーション
    [ ] localAutoLearn.ts に rebuildCalibrationCurve 追加
    [ ] /api/reanalyze 末尾で実行・保存
    [ ] autoLearnFromNewResult から定期再構築呼び出し
[ ] §F 会場×距離複合キー
    [ ] HorseStat.courseDistData 追加（migrate）
    [ ] horseStats.ts で複合キー集計
    [ ] /api/learn /api/reanalyze に merge 追加
    [ ] scorer に getCourseDistBonus 追加
    [ ] LocalWeights.courseDistMult, _bonuses.courseDist 追加
[ ] §G 馬体重三軸
    [ ] HorseStat.avgHorseWeight, weightSamples 追加（migrate）
    [ ] horseStats.ts で平均体重集計
    [ ] scorer の getWeightAbsoluteBonus 呼び出しに stat.avgHorseWeight を渡す
    [ ] getSeasonalWeightBonus 追加し weightAbs と合算
[ ] §H 横断
    [ ] LocalWeights / _bonuses 最終形を確認
    [ ] localAutoLearn factorMap 4キー追加
    [ ] getLocalWeights spread 順で defaults fallback
[ ] §I データ再構築
    [ ] migrate / build / reanalyze / predict / learn 一連通る
[ ] §J 検証
    [ ] blind 連対率 ≥ 79.5%
    [ ] /api/predict 応答 < 2秒
[ ] git commit -m "feat: v343 連対率向上7改修（間隔ビン/オッズ乖離/騎手コース/前走人気ギャップ/キャリブレーション/会場距離複合/馬体重三軸）"
```

---

# 付録: ファイル変更マトリクス

| ファイル                                     | §A  | §B  | §C  | §D  | §E  | §F  | §G  | §H  |
| -------------------------------------------- | --- | --- | --- | --- | --- | --- | --- | --- |
| `prisma/schema.prisma`                       | ✓   |     | ✓   | ✓   |     | ✓   | ✓   |     |
| `src/lib/intervalBins.ts` (新)               | ✓   |     |     |     |     |     |     |     |
| `src/lib/jockeyTrainerStats.ts` (新)         |     |     | ✓   |     |     |     |     |     |
| `src/lib/horseStats.ts`                      | ✓   |     |     | ✓   |     | ✓   | ✓   |     |
| `src/lib/scorer.ts`                          | ✓   | ✓   | ✓   | ✓   |     | ✓   | ✓   | ✓   |
| `src/lib/localAutoLearn.ts`                  | ✓   | ✓   | ✓   | ✓   | ✓   | ✓   |     | ✓   |
| `src/app/api/learn/route.ts`                 | ✓   |     |     | ✓   |     | ✓   |     |     |
| `src/app/api/reanalyze/route.ts`             | ✓   |     | ✓   | ✓   | ✓   | ✓   |     |     |
| `src/app/api/predict/route.ts`               |     |     | ✓   |     |     |     |     |     |

以上。各章は独立しているため、§A→§D→§F のように軽い順から順次マージ可能。

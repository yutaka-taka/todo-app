# Local PostgreSQL Database (port 5432, db: keiba)
DATABASE_URL="postgresql://postgres:post@localhost:5432/keiba"

# Anthropic Claude API Key (optional — only used by /api/learn, /api/verify, /api/reanalyze)
# 解約後はローカル自己学習モードで動作するため未設定でも可
ANTHROPIC_API_KEY="sk-ant-..."
